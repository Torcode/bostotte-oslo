---
title: "Prognose for statlig bostøtte i Oslo"
subtitle: "Regelstyrte tidsserier, intervensjonsanalyse og kalibrerte prediksjonsintervaller"
author:
  - name: "Torkild M"
date: last-modified
date-format: "D. MMMM YYYY"
lang: nb
number-sections: true
bibliography: referanser.bib
csl-locale: nb-NO
crossref:
  sec-prefix: "avsnitt"
  eq-prefix: "ligning"
format:
  typst:
    papersize: a4
    fontsize: 10.5pt
    section-numbering: "1.1.1"
    toc: true
    toc-title: "Innhold"
    toc-depth: 3
    citeproc: true
    keep-typ: false
    template-partials:
      - mal/typst-template.typ
execute:
  echo: false
  warning: false
  message: false
  freeze: false
editor: visual
---

```{r}
#| label: oppsett
#| include: false

# =============================================================================
# Oppsett. Én chunk laster miljøet, datapakken og de avledede størrelsene som
# resten av dokumentet — inkludert løpende tekst — refererer til. Ingen tall i
# dette dokumentet er skrevet av for hånd; alle kommer herfra eller fra en
# chunk lenger nede, slik at teksten ikke kan komme i utakt med datapakken.
# =============================================================================

# Sjekk avhengigheter først, med en melding som sier hva som mangler og hva som
# fikser det. Uten dette feiler dokumentet nedstrøms med «could not find
# function \"ARIMA\"», som ikke forteller noen hva de skal gjøre.
.pakker <- c("tidyverse", "knitr", "tsibble", "fable", "fabletools",
             "feasts", "urca", "distributional")
.mangler <- .pakker[!vapply(.pakker, requireNamespace, logical(1), quietly = TRUE)]
if (length(.mangler)) {
  stop("\n\nManglende R-pakker: ", paste(.mangler, collapse = ", "),
       "\n\nKjør source(\"oppsett.R\") i repo-roten, eller:\n  install.packages(c(",
       paste0('\"', .mangler, '\"', collapse = ", "), "))\n", call. = FALSE)
}

suppressPackageStartupMessages({
  library(tidyverse)
  library(knitr)
  library(tsibble)
  library(fable)
  library(fabletools)
  library(feasts)
  library(urca)
  library(distributional)
})

knitr::opts_chunk$set(
  echo = FALSE, message = FALSE, warning = FALSE,
  fig.align = "center", dev = "svg"
)

# --- UTF-8 uavhengig av vertsmaskin ------------------------------------------
# Uten UTF-8 som native encoding konverterer R hver ikke-ASCII-streng til
# vertens tegnsett på vei ut, og skriver <U+00E5> der det skulle stått å. Da
# blir både tabeller og løpende tall uleselige. Locale-navnene er ulike på
# Unix og Windows, så vi prøver begge familier.
#
# Vakten STOPPER dersom UTF-8 ikke lar seg sette. Den forrige versjonen lot
# dokumentet fortsette, og produserte en PDF der hvert tusenskille sto som
# <U+00A0>. En vakt som ikke stopper er ingen vakt.
# Vi tester symptomet, ikke navnet paa locale: taaler en norsk bokstav og et
# hardt mellomrom en tur gjennom vertens tegnsett uten aa bli escape't? Det er
# noeyaktig det som avgjoer om PDF-en blir lesbar.
.tegnsett_ok <- function() {
  s <- "\u00e5\u00a0"
  !grepl("<U+", enc2native(s), fixed = TRUE)
}
if (!.tegnsett_ok()) {
  for (loc in c("nb_NO.UTF-8", "nb_NO.utf8",                   # Unix
                "Norwegian Bokmal_Norway.utf8",                # Windows
                "Norwegian_Norway.utf8", "Norwegian.utf8",
                "English_United States.utf8",
                "C.UTF-8", "C.utf8", "en_US.UTF-8")) {
    if (.tegnsett_ok()) break
    suppressWarnings(try(Sys.setlocale("LC_ALL", loc), silent = TRUE))
  }
}
if (!.tegnsett_ok()) {
  stop("\n\nR kjorer ikke med UTF-8 som tegnsett (locale: ",
       Sys.getlocale("LC_CTYPE"), ").",
       "\nDokumentet inneholder norske tegn, og uten UTF-8 skriver R dem",
       "\nsom <U+00E5> i tabeller og som <U+00A0> i hvert tusenskille.",
       "\nPDF-en ville blitt bygget, men vart uleselig.",
       "\n\nWindows: Innstillinger > Tid og sprak > Sprak >",
       "\n  Administrative sprakinnstillinger > Endre systemets",
       "\n  omradeinnstilling > kryss av for \"Beta: Bruk Unicode UTF-8\",",
       "\n  og start maskinen pa nytt.",
       "\nUnix: start R med LANG=nb_NO.UTF-8 eller LANG=C.UTF-8.",
       "\n\nRepoets .Rprofile forsoker dette automatisk ved oppstart. Slar",
       "\ndet feil, er det systeminnstillingen over som ma endres.\n",
       call. = FALSE)
}

# --- Formatering: norske tallkonvensjoner ------------------------------------
# Tusenskille er hardt mellomrom (U+00A0) slik at 20 849 ikke brekker over to
# linjer. Tegnet skrives som R-escape, ikke som byte i kildefila: en literal
# U+00A0 i koden gir en streng R kan komme til å escape'e på vei ut, og da
# står det <U+00A0> i PDF-en i stedet for et mellomrom.
NBSP <- "\u00a0"

# Ikke-endelige tall stopper byggingen framfor å bli trykt. Bakgrunnen er
# konkret: en 0/0 i sanntidskanten sto som NaN i løpende tekst i en tidligere
# PDF. Ni regnskapsidentiteter stoppet byggingen, men ingen av dem så på et
# enkelt inline-tall. Nå gjør formateringsfunksjonen selv den kontrollen.
.endelig <- function(x, hvor) {
  if (!all(is.finite(x)))
    stop("\n\n", hvor, "(): fikk en verdi som ikke er et tall: ",
         paste(utils::head(x[!is.finite(x)], 3), collapse = ", "),
         "\nEt tall i teksten er NA, NaN eller Inf. Rett kilden framfor",
         "\na trykke det.\n", call. = FALSE)
  invisible(TRUE)
}
nb  <- function(x, d = 0) {
  .endelig(x, "nb")
  formatC(x, format = "f", digits = d, big.mark = NBSP, decimal.mark = ",")
}
pst <- function(x, d = 1) paste0(nb(x, d), NBSP, "%")
mnd <- function(x) paste0(lubridate::year(x), "m", lubridate::month(x))
kab <- function(x, ...) {
  knitr::kable(x, format.args = list(big.mark = NBSP, decimal.mark = ","), ...)
}

# --- Datapakken ---------------------------------------------------------------
# Finn datapakken robust. Arbeidsmappa under rendering er ikke nødvendigvis den
# samme som dokumentets mappe — den avhenger av om det renderes fra konsollen,
# fra Render-knappen, fra «quarto preview» eller fra CI. Vi prøver arbeidsmappa
# først, deretter dokumentets egen mappe.
#
# Kandidaten må ikke bare finnes, den må la seg åpne. Ligger mappa i OneDrive
# kan file.exists() være sann for en fil som ennå ikke er lastet ned lokalt, og
# da feiler source() med «cannot open the connection» uten å si hvorfor. Vi
# prøver derfor å åpne fila, og skiller de to feilene i meldingen.
.leselig <- function(f) {
  con <- suppressWarnings(tryCatch(file(f, "r", encoding = "UTF-8"),
                                   error = function(e) NULL))
  if (is.null(con)) return(FALSE)
  on.exit(close(con), add = TRUE)
  isTRUE(tryCatch({ readLines(con, n = 1); TRUE }, error = function(e) FALSE))
}
.dokumentmappe <- tryCatch(dirname(knitr::current_input(dir = TRUE)),
                           error = function(e) getwd())
DATASTI <- NULL
.ulesbar <- character()
for (.k in unique(c("velferdsetaten-data",
                    file.path(.dokumentmappe, "velferdsetaten-data"),
                    file.path(getwd(), "velferdsetaten-data")))) {
  .f <- file.path(.k, "scripts", "velferdsetaten_data.R")
  if (!file.exists(.f)) next
  if (.leselig(.f)) { DATASTI <- .k; break }
  .ulesbar <- c(.ulesbar, .f)
}
if (is.null(DATASTI)) {
  stop("\n\nFant ingen lesbar datapakke «velferdsetaten-data/».",
       "\n  Arbeidsmappe:       ", getwd(),
       "\n  Dokumentets mappe:  ", .dokumentmappe,
       if (length(.ulesbar))
         paste0("\n\nFila finnes, men lar seg ikke åpne:\n  ",
                paste(.ulesbar, collapse = "\n  "),
                "\n\nDet skjer når prosjektmappa ligger i OneDrive og filene bare",
                "\nfinnes i skyen. Høyreklikk mappa i Utforsker og velg",
                "\n«Behold alltid på denne enheten», og render på nytt.\n")
       else
         paste0("\n\nÅpne Velferdsprosjekt.Rproj i RStudio og render derfra, ",
                "eller kjør\n  quarto render bostotte_oslo.qmd\nfra repo-roten.\n"),
       call. = FALSE)
}
source(file.path(DATASTI, "scripts", "velferdsetaten_data.R"))
d <- last_alt(DATASTI)

UTTREKKSDATO <- "4. august 2026"   # vintage; jf. avsnitt om proveniens
EST_START    <- as.Date("2017-01-01")

# --- Utfallsserien ------------------------------------------------------------
# Sanntidskanten (siste måned, der terminkjøringen ennå ikke er gjort og
# verdien derfor er en strukturell null) fjernes her. Det er vaskeinngrep V1,
# dokumentert i @tbl-vask.
oslo_raa <- d$oslo |>
  transmute(dato,
            M        = ant_husstander_termin,
            soknader = ant_soknader,
            avslag   = ant_avslag,
            snitt    = gjsnitt_bostotte)

kant_rader  <- oslo_raa |> filter(dato == max(dato), M == 0)
oslo_termin <- oslo_raa |> anti_join(kant_rader, by = "dato")
oslo_est    <- oslo_termin |> filter(dato >= EST_START)

# Landsserien trimmes etter samme regel som Oslo-serien (V1), og av samme grunn:
# siste rad er strukturell null i begge, og 0/0 gir NaN. Oslos andel av
# mottakerne nasjonalt brukes til aa skalere nasjonale forhaandsanslag ned til
# Oslo i M7 (@sec-met-m7), og maa derfor regnes paa terminer med data i begge.
nasj_raa    <- d$nasjonalt |> transmute(dato, M = ant_husstander_termin)
nasj_termin <- nasj_raa |>
  anti_join(nasj_raa |> filter(dato == max(dato), M == 0), by = "dato")

osloandel <- oslo_termin |>
  inner_join(nasj_termin |> rename(M_nasj = M), by = "dato") |>
  arrange(dato) |> tail(12) |>
  summarise(a = 100 * mean(M / M_nasj)) |> pull(a)
stopifnot(is.finite(osloandel), osloandel > 5, osloandel < 40)

# --- Skalarer som brukes i løpende tekst -------------------------------------
n_est     <- nrow(oslo_est)
est_fra   <- mnd(min(oslo_est$dato));  est_til <- mnd(max(oslo_est$dato))
serie_fra <- mnd(min(oslo_termin$dato)); serie_til <- mnd(max(oslo_termin$dato))
n_serie   <- nrow(oslo_termin)

M_snitt <- mean(oslo_est$M); M_sd <- sd(oslo_est$M)
M_cv    <- 100 * M_sd / M_snitt
M_min   <- min(oslo_est$M);  M_maks <- max(oslo_est$M)

topp      <- oslo_est |> slice_max(M, n = 1)
apr24     <- oslo_est |> filter(dato %in% as.Date(c("2024-03-01", "2024-04-01")))
apr24_pst <- 100 * (apr24$M[2] / apr24$M[1] - 1)

# Volatilitet: gjennomsnittlig absolutt månedsendring i prosent, før og etter
# skjermingen (termin januar 2025). Brukes i @sec-met-observasjoner og @fig-vol.
mad_pst <- function(x) {
  x <- arrange(x, dato)
  p <- abs(100 * (x$M / lag(x$M) - 1))
  c(for_ = mean(p[x$dato <  as.Date("2025-01-01")], na.rm = TRUE),
    etter = mean(p[x$dato >= as.Date("2025-01-01")], na.rm = TRUE))
}
vol_oslo <- mad_pst(oslo_est)

bg_est <- d$brukergruppe |>
  transmute(brukergruppe, dato, M = ant_husstander_termin) |>
  filter(dato >= EST_START, dato <= max(oslo_est$dato))

vol_bg <- bg_est |>
  group_split(brukergruppe) |>
  map_dfr(\(g) tibble(brukergruppe = g$brukergruppe[1],
                      for_ = mad_pst(g)[["for_"]], etter = mad_pst(g)[["etter"]])) |>
  mutate(faktor = for_ / etter)

BEH <- "Husstander med midlertidige trygdeytelser"
vol_beh   <- vol_bg |> filter(brukergruppe == BEH)
vol_andre <- vol_bg |> filter(brukergruppe != BEH)
```

# Introduksjon

Tilgang til en egnet bolig er både et grunnleggende velferdsgode og en forutsetning for deltakelse i arbeids- og samfunnsliv. I artikkel 25 i FNs verdenserklæring om menneskerettigheter inngår bolig i retten til en levestandard som er tilstrekkelig for individets og familiens helse og velferd (FN, 1948). Anerkjennelsen av bolig som et grunnleggende gode innebærer imidlertid ikke at markedet automatisk sikrer alle en egnet bolig.

Under strenge forutsetninger viser det første velferdsteoremet at en frikonkurranselikevekt er Pareto-effektiv. Pareto-kriteriet er imidlertid en ufullstendig rangering: det skiller ikke mellom fordelinger, og en svært ulik allokering kan være effektiv. Hvilken effektiv likevekt markedet faktisk leverer, bestemmes dessuten av den underliggende fordelingen av inntekt og formue. En boligmarkedslikevekt kan dermed være effektiv i økonomisk forstand samtidig som husholdninger med lave inntekter ikke har råd til en bolig som tilfredsstiller grunnleggende behov. Dette skillet — markedet sørger i beste fall for effektivitet, mens fordelingen krever egne virkemidler — står sentralt i begrunnelsen for offentlige boligpolitiske virkemidler. Gjennom bostøtte omfordeler staten kjøpekraft til husstander med lave inntekter og høye boutgifter, uten å erstatte boligmarkedets prismekanisme — i praksis en anvendelse av innsikten fra det andre velferdsteoremet, med et forbehold kapittel 2 formaliserer: bostøtten er behovsprøvd, ikke en lump-sum-overføring.

Problemstillingen er særlig aktuell i Oslo. Ifølge Statistisk sentralbyrå har Oslo landets høyeste husleienivå. I 2025 var gjennomsnittlig månedlig leie for en toroms bolig i Oslo og Bærum 15 260 kroner, 29 prosent over landsgjennomsnittet. Kombinasjonen av høye boutgifter og store forskjeller i husholdningenes inntekter innebærer at markedsløsningen alene ikke nødvendigvis gir en sosialt ønskelig fordeling av boligkonsumet. Statlig bostøtte er derfor et sentralt virkemiddel for å redusere boutgiftsbelastningen blant økonomisk vanskeligstilte husstander.

For Velferdsetaten er bostøtten imidlertid ikke bare et virkemiddel som skal begrunnes, men en størrelse det skal planlegges rundt. Ordningen er statlig finansiert og forvaltet av Husbanken, men kommunen er førstelinje, og mottakstallet inngår i kunnskapsgrunnlaget for tilgrensende kommunale tjenester som økonomisk sosialhjelp og kommunal bostøtte. Størrelsesordenen på det kommunale behovet kartlegges annethvert år: i uke 48 i 2024 registrerte bydelene 3 826 husstander med behov for kommunal hjelp til å skaffe bolig, 480 flere enn i 2022 [@velferdsetaten2025bolig]. Statlig bostøtte virker i grenseflaten mot dette behovet, og hvor mange husstander som mottar den de kommende månedene, har dermed betydning for dimensjonering av veiledning og saksbehandling i bydelene og for tidlig varsling når utviklingen avviker fra det forventede. Dette arbeidet anlegger derfor prognoseperspektivet.

En prognose for bostøttemottak står overfor en særegen metodisk utfordring: serien er regelstyrt. Antall mottakere bestemmes ikke bare av inntekter, husleier og søkeatferd, men av hvordan retten til bostøtte til enhver tid er definert. Regelverket er endret flere ganger i seriens levetid: midlertidig utvidede satser og ekstraordinær strømstøtte fra termin desember 2021 til og med termin mars 2024, tilbakeføring til ordinært regelverk fra termin april 2024, økte boutgiftstak og forenklet egenandelsberegning fra juli 2024 med ytterligere utvidelser utover høsten, og fra januar 2025 en korreksjon av inntektsgrunnlaget som skal motvirke et administrativt skapt sesongfall for mottakere av arbeidsavklaringspenger og dagpenger. En modell som ikke kjenner disse endringene, vil lese vedtak som trend og utbetalingskalender som naturlig sesong, og fremskrive begge deler feil.

Forskningsspørsmålet i dette arbeidet er derfor: hvor presist kan månedlig antall husstander som mottar statlig bostøtte i Oslo prognostiseres 1 til 12 måneder frem ved hjelp av åpne data, og i hvilken grad forbedres prognosenes treffsikkerhet og prediksjonsintervallenes dekning når kjente regelendringer modelleres eksplisitt? To underspørsmål følger. Det første (**UA**) er retrospektivt og deskriptivt: hvordan sammenfaller regelendringene i 2021–2025 med endringer i seriens nivå, sesongmønster og usikkerhet? Det andre (**UB**) gjelder drift: hvordan bør en prognosemodell i løpende bruk håndtere permanente, midlertidige og kalenderrelaterte regelverksendringer, medregnet endringer som er vedtatt, men ennå ikke trådt i kraft?

\newpage

# Teori og begrepsapparat {#sec-teori}

Kapitlet bygger tre lag: ordningens mekanikk (hvem som er innenfor, og hvorfor), atferden og institusjonene som avgjør hvem som faktisk mottar, og tidsserieteorien som oversetter begge til en prognosemodell. Siteringsprinsippet er enkelt: alt som går forbi det intuitive, bæres av fagfellevurdert litteratur eller primærkilder; det intuitive bærer seg selv gjennom ligningene.

## Ordningen: mekanikk og nøkkelbegreper {#sec-teori-mekanikk}

Bostøtte er en statlig, rettighetsbasert og behovsprøvd månedlig ytelse til husstander med lave inntekter og høye boutgifter, hjemlet i bustøttelova og operasjonalisert i forskrift om bustøtte, forvaltet av Husbanken med kommunen som førstelinje [@bustottelova2012; @bostotteforskrift2012]. Dagens ordning stammer fra 2009-reformen. En regjeringsoppnevnt ekspertgruppe konkluderte i 2022 med at ordningen er viktig for mange, men fremstår som vanskelig å forstå, tilfeldig og usammenhengende [@ekspertgruppe2022; @pedersen2023], og et nytt offentlig utvalg skal levere en helhetlig gjennomgang i desember 2026 [@bostottealliansen2026]. Ordningen er altså både empirisk volatil og politisk i bevegelse — begge deler er prognoseproblemets kjerne.

| Begrep | Innhold |
|------------------------------------|------------------------------------|
| Statlig bostøtte | Månedlig, behovsprøvd rettighetsytelse fra Husbanken [@bustottelova2012]. |
| Husstand | Alle folkeregistrert på adressen. Inntekten til husstandsmedlemmer fra 18 år inngår i grunnlaget; fra termin oktober 2024 er hjemmeboende 18–19-åringers inntekt unntatt, mens søkers egen inntekt alltid inngår [@husbankenInntekter; @husbanken2024rnb]. |
| Mottaker (per måned) | Husstand med positivt vedtak og utbetaling for terminen — en *beholdningsstørrelse*, ikke en strøm [@husbankenStatistikk]. |
| Termin | Kalendermåned. Søknadsfrist den 25.; utbetaling etterskuddsvis den 20. i måneden etter [@husbankenKanFaa]. |
| Godkjente boutgifter ($BU$) | Husleie, eller renter/avdrag og driftsutgifter i eid bolig, opp til boutgiftstaket [@husbankenBoutgifter]. |
| Boutgiftstak ($\tau$) | Øvre grense for boutgifter i beregningen; varierer med husstandsstørrelse og kommunegruppe, der Oslo ligger høyest [@bostotteforskrift2012]. |
| Vektet inntekt ($\tilde y$) | Husstandens brutto månedsinntekt (inkl. formuestillegg) delt på en husstandsvekt: første person 1,0, øvrige 0,13 (over 18 år) eller 0,15 (under) [@bostotteforskrift2012]. |
| Egenandel ($E(\tilde y)$) | Inntektsavhengig del av boutgiftene husstanden dekker selv; progressiv i vektet inntekt. Den trinnvise beregningen ble erstattet av et lineært ledd fra termin juli 2024 [@husbankenBeregning; @husbanken2024aars]. |
| Dekningsgrad ($\kappa$) | 73,7 % av differansen mellom godkjente boutgifter og egenandel (forskrift om bustøtte § 1) [@bostotteforskrift2012; @husbankenBeregning]. |
| Inntektsgrunnlag | Faktiske bruttoinntekter per måned, hentet automatisk fra a-ordningen; kapitalinntekt over fribeløp fordeles på tolv måneder [@husbankenInntekter]. |
| Meldekortytelser | Dagpenger og AAP utbetales per 14. dag; normalt to måneder i året faller tre utbetalinger i samme kalendermåned [@kdd2025]. |
| Take-up ($\theta$) | Andelen berettigede husstander som faktisk mottar; komplementet er ikke-bruk [@vanoorschot1991]. |
| Intervensjon | Datert regelendring modellert som puls, trinn eller gradvis innfaset effekt [@boxtiao1975]. |
| Kalendereffekt | Deterministisk variasjon skapt av kalenderens struktur, kjent på forhånd [@bellhillmer1983]. |
| Prediksjonsintervall | Intervall som skal dekke realisasjonen med nominell sannsynlighet; kalibrering betyr empirisk dekning ≈ nominell [@gneiting2007]. |

: Nøkkelbegreper med presise definisjoner

Hele utmålingen er én linje:

$$B_{it} \;=\; \kappa \,\Big[\min\big(BU_{it},\,\tau_{g(i),\,n(i)}\big) \;-\; E(\tilde y_{it})\Big]_{+},
\qquad \kappa = 0{,}737$$ {#eq-bostotte}

der $[\cdot]_+$ betyr at negative beløp settes til null, $g(i)$ er kommunegruppe og $n(i)$ husstandsstørrelse. Intuisjonen: staten dekker knappe tre fjerdedeler av gapet mellom det boligen koster (opp til et tak) og det husstanden selv forventes å bære gitt inntekten. Ordningen har dermed bare to håndtak — taket $\tau$ og egenandelskurven $E(\cdot)$ — og alle regelendringer virker gjennom ett av dem.

Berettigelse følger direkte av @eq-bostotte. Den *implisitte inntektsgrensen* er punktet der egenandelen spiser hele det godkjente boutgiftsnivået:

$$\tilde y^{*}:\; E(\tilde y^{*}) = \min(BU,\tau)
\quad\Longrightarrow\quad
\text{berettiget} \iff \tilde y < \tilde y^{*}$$ {#eq-grense}

Antall berettigede i måned $t$ er massen av husstander under grensen i inntektsfordelingen, $E_t = N_t \cdot F_t(\tilde y^{*}_t)$. To konsekvenser er sentrale for prognose. For det første er enhver satsendring en forskyvning av $\tilde y^{*}$; effekten på antallet er omtrent *tettheten av husstander ved grensen ganger forskyvningen*. Store brudd i serien oppstår altså ikke fordi regelendringen er stor i kroner, men fordi mange husstander ligger tett inntil grensen. For det andre virker egenandelen som en implisitt marginalskatt på inntekt innenfor ordningen, $\partial B/\partial \tilde y = -\kappa\,E'(\tilde y)$ — en velkjent arbeidsinsentivmekanisme som også er dokumentert å forlenge mottaksvarighet i den norske ordningen [@nordvik2005].

## Hva serien måler: beholdning, strømmer og måleregimer {#sec-teori-beholdning}

Mottakertallet er en beholdning som bare kan endres av strømmer:

$$M_t = M_{t-1} + I_t - U_t$$ {#eq-stockflow}

Persistens — høy autokorrelasjon — er derfor seriens naturtilstand og enhver naiv prognoses styrke; informasjonen ligger i det som flytter inn- ($I_t$) og utstrømmen ($U_t$). Koblet mot berettigelse og atferd:

$$M_t = \theta_t \, E_t,
\qquad
\Delta \ln M_t = \Delta \ln \theta_t + \Delta \ln E_t$$ {#eq-takeup}

Regelverk, inntektsutvikling og demografi flytter $E_t$; informasjon, byrder og stigma flytter $\theta_t$. Dekomponeringen er analysens ryggrad: den skiller det som kan leses ut av forskriften fra det som må estimeres fra data.

Fire institusjonelle fakta bestemmer seriens dynamikk. (1) Behovsprøvingen skjer *hver måned mot faktiske bruttoinntekter* hentet automatisk fra a-ordningen — inntektsstøy overføres dermed direkte til mottaksstatus [@husbankenInntekter]. (2) Positive vedtak videreføres og omregnes maskinelt hver termin [@husbanken9c22]; utstrømming ved innstramming er derfor automatisk og umiddelbar. (3) Innstrømming krever aktiv søknad innen den 25. [@husbankenKanFaa]; respons på utvidelser filtreres gjennom $\theta$ og kommer med treghet. (4) Før 2017 ble inntekt oppdatert årlig med ligningstall i juli, med systematiske juli-fall som resultat — i 2016 fra 112 000 til 98 800 mottakere nasjonalt [@husbanken2016]. Overgangen til månedsinntekt er et *måleregimeskifte*: serien før og etter 2017 er ikke samme datagenererende prosess, og estimeringsutvalget avgrenses derfor til 2017 og senere.

Av (2) og (3) følger kapitlets asymmetriproposisjon: *negative regelendringer gir skarpe trinn i serien; positive gir gradvis innfasing.* Den formaliseres i @eq-transfer og testes empirisk.

## Atferd og samfunn: hvorfor $\theta < 1$ — og hvorfor den beveger seg {#sec-teori-atferd}

Ikke-bruk er regelen, ikke unntaket, i behovsprøvde ordninger internasjonalt; take-up-rater på 40–80 % er vanlige på tvers av land og programmer [@hernanz2004; @currie2004; @ko2022]. Begrepsapparatet skiller primær ikke-bruk (søker aldri) fra sekundær (faller ut underveis) [@vanoorschot1991]. For den norske bostøtten er det anslått at 50 000–70 000 berettigede husstander ikke mottar støtte [@leieboerforeningen2022] — samme størrelsesorden som hele mottakerbestanden i Oslo-regionen, og en påminnelse om at $\theta$ er en fri parameter, ikke en konstant lik én.

Den økonomiske grunnmodellen behandler stigma som en kostnad i deltakelsesbeslutningen: husstanden søker hvis nytten av beløpet overstiger summen av praktiske og psykologiske kostnader [@moffitt1983], med sosiologisk rot i Goffmans analyse av stigma som ødelagt identitet [@goffman1963]. Antropologien om forvaltningens førstelinje føyer til at faktisk tilgang formes i møtet med bakkebyråkraten — skjønnet i skranken *er* politikk i praksis [@lipsky2010]; for bostøtten er kommunen denne førstelinjen.

Atferdsøkonomien har flyttet feltet fra å postulere kostnadene til å måle dem. Et felteksperiment med 35 000 amerikanske husholdninger viste at forenklet informasjon alene økte take-up vesentlig — kompleksitet og psykologiske friksjoner holder folk ute selv når beløpene er store [@bhargava2015]. Knapphet beslaglegger dessuten kognitiv båndbredde nettopp hos målgruppen, slik at de som trenger ordningen mest, har minst kapasitet til å navigere den [@shah2012; @mullainathan2013]. Statsvitenskapen samler dette som *administrative byrder* — lærings-, etterlevelses- og psykologiske kostnader som fungerer som politikkutforming med andre midler [@moynihan2015; @herd2018]. Og friksjonene er ikke fordelingsnøytrale: eksperimentelt øker assistanse både take-up og endrer *hvem* som kommer inn i ordningen [@finkelstein2019]. Ekspertgruppens karakteristikk av det norske regelverket som vanskelig å forstå [@ekspertgruppe2022] er i dette lyset ikke en stilanmerkning, men en take-up-mekanisme.

Norsk empiri rammer inn ordningen som et tvetydig instrument mellom boligpolitikk og inntektssikring [@nordvik2014; @fjelltoft2024], med egenandelsprinsippene analysert i egen utredning [@astrup2024; @menon2020]. Husbankens egen treffsikkerhetsanalyse for 2025 stiller det spørsmålet denne studien låner sitt begrep fra — i hvilken grad ordningen faktisk setter husstander med lave inntekter og høye boutgifter i stand til å bo egnet — og gjennomgår samtidig konsekvensene av regelendringene de siste årene [@fjelltoft2025]. Den analysen er tverrsnittsorientert; dette arbeidet stiller det komplementære spørsmålet om samme regelendringer lar seg *framskrive*. Husbankens egen samspillsanalyse dokumenterer to drivere med direkte prognoserelevans: økende overlapp med økonomisk sosialhjelp, og at bosatte fordrevne fra Ukraina løftet mottaket nasjonalt fra om lag 5 100 husstander i 2022 til 18 400 i 2024 — et rent sammensetningssjokk i $E_t$ [@husbanken2025samspill]. Til slutt incidensen: internasjonal forskning viser at deler av en bostøtte kan kapitaliseres i husleiene [@fack2006; @gibbons2006; @eerola2021; @kemp2007]. For denne studien er incidens andreordens på 12-måneders horisont, men den virker på sikt gjennom boutgiftene og dermed grensen $\tilde y^{*}$.

Fire prognoseimplikasjoner følger. (i) *Mekanisk ≠ realisert:* effekten av en regelendring kan ikke regnes ut fra forskriften alene — $\theta$ demper og forsinker; intervensjonseffekter må estimeres. (ii) *Asymmetri:* jf. @sec-teori-beholdning — automatisk utstrømming, søknadsbasert innstrømming. (iii) *Salience:* kriser og medieoppmerksomhet (strømkrisen 2021–2023) kan løfte $\theta$ midlertidig og modelleres som transiente effekter. (iv) *Sammensetning:* bosettingssjokk flytter $E_t$ uavhengig av regelverket og motiverer eksterne kontrollserier.

## Tidsserieteori for en administrativ beholdningsserie {#sec-teori-tsteori}

Serien dekomponeres i en strukturell modell i state space-form [@harvey1989; @durbin2012]:

$$y_t = \mu_t + \gamma_t + \sum_j f_j(t) + \beta\, k_t + \varepsilon_t$$ {#eq-ssm}

der $\mu_t$ er en lokal lineær trend (random walk med drift), $\gamma_t$ stokastisk sesong, $f_j$ intervensjoner, $k_t$ kalenderregressoren og $\varepsilon_t$ støy. Kalmanfilteret gir rekursiv estimering og 1–12-måneders prognoser med konsistent usikkerhet. ETS og SARIMA er de veletablerte referanseklassene i samme familie [@hyndman2002; @box2015; @hyndman2021].

**Intervensjoner.** Kjente, daterte regelendringer modelleres etter @boxtiao1975 med transferfunksjon:

$$f(t) = \frac{\omega}{1-\delta L}\, S_t(t_0),
\qquad
\text{langtidseffekt} = \frac{\omega}{1-\delta}$$ {#eq-transfer}

der $S_t(t_0)$ er et trinn fra tidspunkt $t_0$ og $L$ lag-operatoren. $\delta = 0$ gir et rent trinn — forventningen for negative brudd, jf. asymmetriproposisjonen; $\delta \in (0,1)$ gir gradvis innfasing — forventningen for utvidelser; en puls fanger engangseffekter. Regelverkskalenderen for 2024 gir intervensjonsanalysen både sitt skarpeste brudd og sin vanskeligste identifikasjon. Fra termin april falt den midlertidig reduserte egenandelen og den ekstraordinære strømstøtten bort — departementet anslo på forhånd et fall på 20 000–25 000 mottakere nasjonalt [@dagsavisen2024; @pensjonistforbundet2024], og Husbankens årsrapport bekrefter i etterkant at om lag 25 000 husstander mistet retten ved avviklingen [@husbanken2024aars]. Utover høsten fulgte tre utvidelser med to til tre måneders mellomrom: hevet boutgiftstak fra termin juli (første utbetaling i august, siden bostøtten betales etterskuddsvis), oppvarmingstillegg til alle mottakere fra termin september, og fra termin oktober ble inntekten til hjemmeboende 18–19-åringer tatt ut av husstandsinntekten [@husbanken2024aars; @husbanken2024rnb]. April-bruddet er datert og identifiserbart; høsttrinnene ligger for tett på hverandre til å skilles empirisk i en serie på `r nb(n_est)` observasjoner, og estimeres derfor samlet i metodekapitlet — et identifikasjonsvalg, ikke en teoretisk begrensning.

**Kalendereffekter.** Deterministisk kalendervariasjon skal modelleres med kjente regressorer, ikke overlates til sesongkomponenten [@bellhillmer1983; @findley1998]. Bostøttens kalendereffekt er meldekortsyklusen: dagpenger og AAP utbetales per 14. dag, så $u_t \in \{2,3\}$ utbetalinger treffer kalendermåned $t$, med 26 utbetalinger på 12 måneder. Før 2025 telte alt som månedsinntekt, og mottakere ble skjøvet over inntektsgrensen i nettopp tre-utbetalingsmånedene [@kdd2025]. Regressoren blir

$$k_t = (u_t - \bar u)\,\mathbf{1}\{t \le \text{des. } 2024\},
\qquad \bar u = \tfrac{26}{12}$$ {#eq-meldekort}

Fra januar 2025 regnes bare to tredeler av slik inntekt med i tre-utbetalingsmåneder [@husbankenInntekter; @kdd2025]. Mekanikken er eksakt: tre utbetalinger à to tredeler tilsvarer to utbetalinger — telt inntekt i en tre-utbetalingsmåned blir identisk med en normalmåned. Gitt korrekt administrasjon er kalendereffekten dermed mekanisk nøytralisert, og koeffisienten forventes å være *null* etter regimeskiftet — en skarp, testbar prediksjon, ikke en antakelse. Det avgjørende prognosepoenget: NAVs utbetalingskalender er publisert på forhånd [@navUtbetaling], så $k_t$ er *perfekt kjent på alle horisonter* — regressoren forbedrer både punktprognoser og intervaller uten selv å tilføre usikkerhet. Dette er samme arkitektur som påske- og handledagsregressorene i offisiell sesongjustering [@findley1998], anvendt på en trygdeadministrativ syklus.

**Brudd er hovedfienden.** Prognoselitteraturens sentrale lærdom er at prognosesvikt først og fremst skyldes umodellerte deterministiske nivåskift [@clements1998]. Alternativet til eksplisitt intervensjonsmodellering er at trend- og sesongkomponentene «spiser» bruddet — som forgifter både punktprognoser og intervaller lenge etter selve hendelsen. Det er den teoretiske begrunnelsen for studiens andre hovedspørsmål: gevinsten av å modellere kjente regelendringer eksplisitt.

## Modellklasser: fra referanse til PhD+ {#sec-teori-modeller}

Med `r nb(n_est)` månedsobservasjoner i estimeringsutvalget (`r est_fra`–`r est_til`) er modellvalget et spørsmål om struktur per parameter, ikke om fleksibilitet. Hierarkiet:

1.  **Referanser:** naiv, sesongnaiv, ETS og SARIMA [@hyndman2021; @box2015]. Enhver mer kompleks modell må slå disse i ekte ut-av-utvalg-test; M4-konkurransens hovedfunn er at enkle statistiske metoder og kombinasjoner er svært vanskelige å slå på enkeltserier [@makridakis2020].
2.  **Hovedklasse:** strukturell modell/RegARIMA med intervensjoner og kalenderregressor (@eq-ssm til @eq-meldekort) — standardarkitekturen i offisiell statistikk for nettopp denne problemklassen [@findley1998; @harvey1989].
3.  **Bayesiansk strukturell tidsserie (BSTS):** samme tilstandsromkjerne med spike-and-slab-prior for automatisk regressorvalg [@scott2014], og med kontrafaktisk konstruksjon for effektestimering [@brodersen2015]. Poenget er koblingen, ikke kompleksiteten: ett rammeverk besvarer både prognosespørsmålet (1–12 måneder frem) og effektspørsmålet (hvor stort var april 2024-bruddet, gitt kontrafaktisk bane?) med konsistent usikkerhet.
4.  **ML-referanse:** gradientboosting [@chen2016]. M5 viste at maskinlæring vinner ved massiv tverrlæring over titusenvis av beslektede serier [@makridakis2022]; én Oslo-serie, eventuelt pluss femten bydeler, er ikke M5. Prophet inkluderes som utbredt industristandard [@taylor2018], med M4-forbeholdet in mente.
5.  **Utvidelse ved bydelsdata:** avstemt hierarkisk prognose med MinT, der bydels- og totalprognoser tvinges konsistente og låner presisjon av hverandre [@wickramasuriya2019].

Avanserte metoder forsvarer altså plassen sin her ved kontekst — de svarer på spørsmål strukturen i problemet faktisk stiller — ikke ved navnet sitt.

## Evaluering: presisjon og kalibrering {#sec-teori-evaluering}

Designet er rullerende opprinnelse: tren på data til $t$, prognoser $t{+}1,\dots,t{+}12$, rull frem, gjenta [@tashman2000]; blokket kryssvalidering av denne typen er den gyldige varianten for avhengige data [@bergmeir2012]. Punktpresisjon måles med MAE/RMSE og den skalafrie

$$\mathrm{MASE}
= \frac{\frac{1}{H}\sum_h |e_{t+h}|}{\frac{1}{T-12}\sum_{t=13}^{T} |y_t - y_{t-12}|}$$ {#eq-mase}

som skalerer feilen mot sesongnaiv prognose i treningsutvalget — verdier under 1 betyr «bedre enn å gjenta fjoråret» [@hyndmankoehler2006]. Forskjeller mellom modeller testes med Diebold–Mariano [@diebold1995] med småutvalgskorreksjon [@harvey1997].

Intervaller evalueres på to akser samtidig: *dekning* (andelen realisasjoner innenfor intervallet skal treffe nominelt nivå) og *skarphet* (smalere er bedre, gitt dekning). Begge samles i intervallskåren

$$S_\alpha(L,U;y) = (U-L) + \tfrac{2}{\alpha}(L-y)\,\mathbf{1}\{y<L\} + \tfrac{2}{\alpha}(y-U)\,\mathbf{1}\{y>U\}$$ {#eq-winkler}

en strengt proper skåringsregel — den kan ikke spilles ved å være systematisk for bred eller for smal [@gneiting2007]. M5-usikkerhetskonkurransen dokumenterer at det er halene som er vanskelige i praksis [@makridakis2022b]. Der modellintervallene underdekker — typisk rundt brudd — kalibreres de konformalt: bruk fordelingen av modellens egne historiske prognosefeil som mal for fremtidig usikkerhet. Tilnærmingen er distribusjonsfri med dekningsgaranti under milde antakelser [@vovk2005; @romano2019], i varianter utviklet for avhengige tidsserier [@xu2023]. Intuisjonen er ett steg: «intervallet er prognosen pluss/minus det feilene faktisk har vært».

## Data: innhentingsprinsipper {#sec-teori-data}

Innhentingen følger en fast stige, øverst først: (1) API; (2) strukturerte filer (CSV/Excel-publiseringer); (3) tabelluttrekk fra PDF; (4) høflig skraping der ingen strukturert kilde finnes — identifisér klienten, respektér robots.txt, cache alle svar; (5) innsynsbegjæring. Prinsippet gjennom alle trinn er proveniens: hver tallkolonne skal kunne regenereres av et navngitt skript fra en navngitt kilde, uten manuell redigering. Det er ikke pedanteri — det er forutsetningen for at intervensjonsdateringene i @sec-teori-tsteori kan etterprøves mot primærkildene.

To egenskaper ved kildebildet bærer designet. Månedsserien for mottakere finnes *ikke* i SSBs statistikkbank — den må hentes fra Husbankens statistikkbank direkte, og uttrekket er dermed kritisk sti for hele studien. Og kalenderregressoren $u_t$ konstrueres deterministisk fra meldekortsyklusens 14-dagersgrid, som gjør den til gratis prognoseinformasjon på alle horisonter. Det operasjonaliserte kildekartet — kilde for kilde, med uttrekksmetode, dekning og navngitt skript — står i @tbl-kildekart i metodekapitlet.

## Fra teori til hypoteser {#sec-teori-syntese}

Teorien over gir fire testbare implikasjoner. **T1:** Beholdningsstrukturen (@eq-stockflow) gjør naive referanser sterke; struktur i form av sesong og kalender skal likevel slå dem systematisk over horisontene 1–12. **T2:** Eksplisitt modellering av de daterte intervensjonene (@eq-transfer) — april 2024-bruddet identifisert for seg, høstutvidelsene 2024 som samlet pakke — reduserer prognosefeil og, viktigere, reparerer intervalldekningen rundt bruddene; dateringene er forankret i primærkilder (forskrift, budsjettvedtak, Husbankens årsrapporter). **T3:** Meldekortregressoren (@eq-meldekort) har negativ og betydelig effekt til og med 2024 og effekt lik null fra 2025 — regimeskiftet er selv en skarp prediksjon modellen kan felles på. **T4:** Nominell intervalldekning oppnås først når brudd og kalender er modellert; residual underdekning korrigeres konformalt, og forbedringen måles med intervallskåren (@eq-winkler). T1–T4 operasjonaliserer hovedspørsmålet og underspørsmålene UA og UB slik de er definert i kapittel 1, og binder dermed atferdsteorien i @sec-teori-atferd, institusjonene i @sec-teori-beholdning og tidsserieteorien i @sec-teori-tsteori til én empirisk plan.

# Metode

Metodekapitlet gjør én kontrakt eksplisitt: hver størrelse som inngår i analysen skal kunne regenereres av navngitt kode fra navngitt kilde, og hver modellbeslutning skal kunne spores tilbake til et argument i @sec-teori eller et dokumentert empirisk faktum. Kapitlet åpner med datagrunnlaget — hva vi ideelt sett burde hatt, hva vi faktisk har, hva forskjellen koster, og hva som er gjort med dataene før de brukes (@sec-met-data). Deretter formaliseres estimanden (@sec-met-estimand), og de deterministiske regressorene bygges: intervensjonsmatrisen (@sec-met-x) og kalenderregressoren (@sec-met-kalender). Modellspesifikasjoner, prognoseprotokoll og evaluering følger i kapitlets andre del.

All kode som produserer tallene nedenfor kjører når dokumentet bygges. Selve koden er utelatt fra PDF-en av lesbarhetshensyn; den ligger i kildefilen `unt_1.qmd` og i `velferdsetaten-data/scripts/`, og hver tabell i dette avsnittet oppgir hvilket objekt den er beregnet fra.

## Datagrunnlaget {#sec-met-data}

### Hva vi ideelt burde hatt, hva vi har, og hva forskjellen koster {#sec-met-ideal}

Det er en nyttig disiplin å skrive ned hvilket datagrunnlag problemet egentlig ber om, før man beskriver det man har. Forskjellen mellom de to er ikke en beklagelse; den er en liste over hvilke spørsmål analysen kan svare på og hvilke den ikke kan.

**Idealet.** Det ideelle datagrunnlaget for dette problemet er et *månedlig husstandspanel over alle husstander i Oslo* — mottakere og ikke-mottakere — med husstandssammensetning og alder, folkeregistrert adresse, bruttoinntekt per måned fra a-ordningen, faktiske boutgifter og disposisjonsform, søknads- og vedtakshendelser med dato og utfall, og regelparameterne som gjaldt i hver termin. Et slikt panel ville gitt fire ting aggregatet ikke kan gi:

1.  *Take-up ville vært observert, ikke antatt.* Berettigelse $E_t$ følger av regelverket anvendt husstand for husstand (@eq-grense); da er $\theta_t = M_t/E_t$ en målt størrelse. Dekomponeringen i @eq-takeup ville vært noe vi kunne estimere, ikke bare resonnere med, og vi kunne sagt om en regelendring flyttet berettigelse eller atferd.
2.  *Regelendringer kunne vært simulert i stedet for estimert.* Kjør regelmotoren med gamle og nye parametre på de samme husstandene; differansen er den mekaniske effekten, eksakt. Det som blir igjen i dataene etter at den er trukket fra, er atferdsresponsen. Intervensjonskoeffisientene ville blitt tolkbare, ikke bare prediktive.
3.  *Høstpakken i 2024 ville løst seg opp.* De tre tiltakene traff tre identifiserbare og bare delvis overlappende grupper: husstander på boutgiftstaket, husstander med oppvarmingsutgifter, og husstander med hjemmeboende 18–19-åringer. På husstandsnivå har hvert tiltak sitt eget behandlingsutvalg, og alle tre er identifisert selv om de ligger to til tre måneder fra hverandre. I månedsaggregatet er de kollineære.
4.  *Strømmene ville vært talt, ikke utledet.* $I_t$ og $U_t$ i @eq-stockflow — og dermed asymmetriproposisjonen — er utsagn om inn- og utstrømming. På husstandsnivå telles de direkte.

**Hvorfor vi ikke har det.** Dataene finnes: de ligger i Husbankens saksbehandlingssystem og i a-ordningen. De er personopplysninger, krever hjemmel og utleveringsavtale, og er ikke åpne. Prosjektet er bygget på åpne kilder, og det er et designvalg med to begrunnelser. Det gjør hele kunnskapsgrunnlaget etterprøvbart for enhver, uten tilgang vi selv har fått innvilget; og det speiler arbeidsvilkårene til en analysefunksjon som skal kunne dele metode og tall på tvers av forvaltningen uten å måtte etablere en avtale først. Én ekstra begrensning gjelder uansett tilgang: en ærlig sanntidsevaluering av prognoser krever *historiske vintages* — datasettet slik det så ut på hvert prognosetidspunkt — og de arkiveres ikke.

**Hva vi faktisk får.** Studiens datagenererende objekt er Husbankens bostøttestatistikk: en fullstendig, ikke-utvalgsbasert administrativ registrering av alle husstander med innvilget statlig bostøtte, hentet fra statistikkbankens egen datamodell. «Utvalg» har derfor en annen betydning enn i survey-basert forskning — det er ikke en stikkprøve fra en populasjon, men en avgrensning i tid og aggregeringsnivå av en totaltelling. Vi får månedstall for `r nb(n_serie)` måneder (`r serie_fra`–`r serie_til`) på tre aggregeringsnivåer som summerer eksakt til hverandre: Oslo totalt, femten bydeler pluss en marginal ufordelt node, og fem brukergrupper. Ved siden av mottakstallet følger søknads- og avslagsvolum, utbetalt beløp og fire snittstørrelser.

**Hva forskjellen koster.** Fire konsekvenser, hver med sitt designsvar:

1.  *$\theta$ og $E$ er ikke separat identifisert.* $M = \theta E$ er en dekomponering vi kan resonnere med, men ikke estimere. Enhver estimert intervensjonseffekt er derfor en *netto*effekt på beholdningen, der mekanisk og atferdsmessig respons er sammenblandet. Designsvar: studien prognostiserer $M$ og beskriver sammenfall; den fremmer ingen påstand om take-up-effekter, og underspørsmål **UA** er derfor formulert deskriptivt.
2.  *Ingen ubehandlet sammenlikningsenhet i tid.* Hver regelendring treffer hele landet på samme dato. Det finnes ingen kontrollkommune. Designsvar: intervensjonsanalyse mot en *modellert* kontrafaktisk bane, ikke mot en kontrollgruppe — og siden effektanslaget da avhenger av modellen for kontrafaktisken, får kontrafaktisken en tilstandsromsmodell med eksplisitt usikkerhet framfor en lineær trend (@sec-teori-modeller).
3.  *Sammensetningsendringer er usynlige* med mindre de tilfeldigvis følger brukergruppeinndelingen. Et bosettingssjokk kommer inn som et uforklart nivåskift.
4.  *Hendelser som ligger tettere enn to–tre måneder, er kollineære* i en månedsserie av denne lengden. Designsvar: høstpakken 2024 estimeres samlet (@tbl-intervensjoner).

**Hva aggregatet gir oss som ingen utvalgsundersøkelse ville gitt.** Tre ting, og de bærer designet:

1.  *Totaltelling uten utvalgsusikkerhet.* Hvert tall er populasjonsverdien. En endring på 200 husstander er en reell endring på 200 husstander, ikke støy rundt et estimat. Det er grunnen til at små, systematiske bevegelser — som kalendereffekten i @sec-met-kalender — i det hele tatt er synlige.
2.  *To eksakte disaggregeringer.* Begge summerer per konstruksjon til totalen, og at de gjør det, verifiseres (@tbl-kontroller). Bydelsnivået gir et hierarki for avstemt prognose. Brukergruppenivået gir noe mer: en *mekanismetest*. Skjermingen fra 2025 skal treffe én brukergruppe og bare den. Studien har dermed ingen kontrollgruppe i tidsdimensjonen, men den har en i mekanismedimensjonen — og @sec-met-observasjoner viser at testen slår ut.
3.  *Lengde.* `r nb(n_serie)` måneder totalt, hvorav `r nb(n_est)` i ett stabilt måleregime som spenner over fem daterte regelendringer.

Vi nøyer oss derfor med aggregatet. Designet er bygd for å hente ut det aggregatet faktisk kan bære, og kapitlet sier eksplisitt hvor grensen går: der noe ikke lar seg identifisere, estimeres det samlet — eller ikke i det hele tatt.

### Utfallsserien: hva én rad er {#sec-met-dv}

Utfallsserien er antall unike husstander i Oslo med positivt vedtaksutfall per termin. Telleregelen er ikke en definisjon vi har valgt i etterkant, men målet slik det er definert i statistikkbankens datamodell: `count(distinct HusstandId)` betinget på virkemiddel bostøtte og vedtaksutfall innvilget. Det er verdt å merke seg hva som *ikke* telles: ikke personer, ikke vedtak, ikke utbetalinger — husstander, én gang hver.

**To kalendre.** Bostøtten lever i to kalendre, og presisjon her avgjør hvordan intervensjoner dateres og hva en prognose i drift kan vite. La $M^{T}_{t}$ være antall husstander med innvilget bostøtte *for termin* $t$ — rettighetsmåneden — og $M^{U}_{t}$ antall husstander med *utbetaling* i kalendermåned $t$. Fordi bostøtten utbetales etterskuddsvis den 20. i måneden etter terminen [@husbankenKanFaa], er de to seriene informasjonsmessig samme objekt:

$$M^{U}_{t} \;=\; M^{T}_{t-1}$$ {#eq-kalenderidentitet}

Identiteten er ikke en antakelse: begge kalendrene hentes fra samme kilde med identisk telleregel, og likheten er derfor testbar. Den testes i @tbl-kontroller. Analysen føres i terminkalenderen, fordi regelendringer trer i kraft per termin, inntektsgrunnlaget hentes per terminmåned [@husbankenInntekter] og meldekortsyklusen treffer terminmåneden. Utbetalingskalenderen er den operative rapporteringskalenderen; for dimensjonering og likviditet forskyves prognosene én måned med @eq-kalenderidentitet, uten informasjonstap.

**Sanntidskanten.** Terminkjøringen for måned $m$ skjer først i påfølgende måned. Den siste raden i uttrekket har derfor termin lik null — ikke fordi ingen mottok bostøtte, men fordi kjøringen ikke er gjort. Dette er en *strukturell null*, og håndteres som vaskeinngrep V1 i @tbl-vask.

**Vintage.** Statistikkbanken lastes på nytt hver natt og revideres bakover ved etterkontroll mot skatteoppgjøret [@husbanken9c22]. Uttrekket som ligger til grunn for dette dokumentet, er datostemplet `r UTTREKKSDATO`. Revisjonene behandles som robusthetsspørsmål, ikke som del av hovedprotokollen, siden historiske vintages ikke er offentlig tilgjengelige.

**Personvern.** Kilden prikker celler med færre enn fire husstander. På analysens aggregeringsnivåer forekommer ingen prikking, og det er ikke en påstand vi tar på tro: prikking ville brutt summeringsidentitetene, som verifiseres eksakt (@tbl-kontroller). Ingen persondata behandles, og analysen arver dermed aggregatenes personvernegenskaper.

### Kildene og hvordan de er hentet {#sec-met-kilder}

Innhentingen følger stigen i @sec-teori-data, øverst først: API, strukturerte filer, tabelluttrekk fra PDF, høflig skraping, innsyn. Prinsippet gjennom alle trinn er proveniens — hver tallkolonne skal kunne regenereres av et navngitt skript fra en navngitt kilde, uten manuell redigering. @tbl-kildekart er den operasjonaliserte varianten: hver rad er hentet av et navngitt skript i `velferdsetaten-data/scripts/`, og rådata er arkivert i `data/raw/` slik at enhver kolonne kan etterprøves offline.

| Rolle | Kilde og uttrekksmetode | Skript |
|:-----------------|:--------------------------------------------|:-------------------|
| Utfallsserier | Husbankens statistikkbank — Qlik-appens Engine-API (WebSocket, anonym offentlig tilgang), måldefinisjoner lest ut av appens datamodell; måned × kommune/bydel × brukergruppe [@husbankenStatistikk] | `extract_bostotte_v2.py` |
| Regelverkskalender | Husbankens årsrapporter 2021–2025 [@husbanken2021aars; @husbanken2022aars; @husbanken2023aars; @husbanken2024aars; @husbanken2025aars], regelverksveilederen [@husbankenBeregning; @husbankenInntekter; @husbankenBoutgifter] og budsjettdokumenter; hendelsesdatert [@husbanken2024rnb; @kdd2025] | kuratert: `intervensjonstabell.csv` |
| Regelparametre | Forskriftssatser via regelverksveilederen (Lovdata er maskinstengt; innholdet er identisk); gjeldende satser + endringslogg | `regelparametre_gjeldende.csv` |
| Strømtiltakenes kronebeløp | Årsrapportenes månedstabeller, månedsoppløst [@husbanken2022aars; @husbanken2023aars] | `stromstotte_manedlig.csv` |
| Meldekortkalender $u_t$ | Deterministisk 14-dagersgrid, fasekalibrert mot data (@sec-met-kalender); månedlig og kjent på alle horisonter; i drift: NAVs publiserte datoer [@navUtbetaling] | i analysen |
| Leienivå (driver av $BU$) | SSB 09895, Leiemarkedsundersøkelsen, PxWeb-API; år × prissone × rom [@ssb09895] | `fetch_ssb.py` |
| Husleieindeks | SSB 03013 (KPI etter konsumgruppe, 04.1) og 14710 (totalindeks, bro inn i 2026), månedlig [@ssb03013; @ssb14710] | `fetch_ssb.py` |
| Befolkning og framskriving | SSB 01222 (kvartal) [@ssb01222]; Oslo kommunes statistikkbank BEF004/BEF005 (bydel, år) og BEF036 (framskriving, aggregert fra delbydel); kvartal/år × bydel [@oslostatbank] | `fetch_ssb.py`, `fetch_oslo_statbank.py` |
| Grunnbeløpet $G$ | NAV; årlig, med virkning fra 1. mai [@navGrunnbelop] | `grunnbelop_historisk.csv` |
| Bydelskovariater | Oslo kommunes statistikkbank: AAP (SOS001), uføretrygd (SOS006), sosialhjelp (STØ013/STØ020); år × bydel [@oslostatbank] | `fetch_oslo_statbank.py` |

: Operasjonalisert kildekart. Alle kilder er åpne, og rådata er arkivert i `data/raw/` slik at hver kolonne kan etterprøves offline. Frekvens og tidsdekning står i @tbl-dekning, som er beregnet fra filene selv. {#tbl-kildekart}

To hull dokumenteres like eksplisitt som kildene, fordi et kjent hull er en annen ting enn et oversett hull. NAVs månedsserier per kommune (AAP, dagpenger, helt ledige) foreligger bare som manuelt nedlastbare regneark og holdes utenfor; behandlingsgruppens månedsserie finnes uansett i brukergruppesnittet av utfallskilden. Og kommunal bostøtte i Oslo publiseres ikke som åpen serie — et datagap som i seg selv er et funn om kunnskapsgrunnlaget på feltet.

### Fire former — hva formen bestemmer {#sec-met-former}

Datagrunnlaget består av tjueto datasett. De er ikke tjue ulike ting å lære seg. En *form* sier ikke hva et datasett handler om, men hvordan det er lagret: hvilken enhet én rad representerer — **kornet** — og dermed hvilken operasjon som er lovlig uten videre bearbeiding. Fire former dekker alle sammen. @tbl-former gir for hver av dem kornet, det kanoniske grepet og den karakteristiske feilen formen inviterer til.

| Form | Kornet: én rad er | Kanonisk grep | Karakteristisk feil |
|:---------|:------------------|:------------------|:----------------------------|
| **1 Serie** | én periode; målene ligger side om side i hver sin kolonne | brukes direkte som tidsserie | å lese to kolonner som ligger i ulike kalendre, som samme tidsakse |
| **2 Panel** | én enhet × én periode | filtrér én enhet → serie; aggregér over enheter → total | å aggregere uten å sjekke at panelet er balansert og at aggregatet treffer totalen |
| **3 Kube** | én *celle* i en flerdimensjonal tabell, i langformat | lås hver dimensjon unntatt tid med eksplisitt filter → serie | å glemme én dimensjon og dobbelttelle over den |
| **4 Oppslag** | én ting å slå opp eller joine på | generér regressor, eller join inn som konstant | å behandle den som en serie — «beløp = 0» betyr her *ingen utbetaling*, ikke *manglende data* |

: De fire formene. Tabellen er begrepsdefinisjoner, ikke beregnede tall — den er den eneste tabellen i avsnittet som er skrevet for hånd. {#tbl-former}

*Form 1 — ferdige månedsserier* (`oslo`, `nasjonalt`). Én rad er én kalendermåned med ni mål ved siden av hverandre: to antallsserier i hver sin kalender (`ant_husstander_termin` er rettighetsmåneden og studiens utfall, `ant_husstander_utbetaling` er kassamåneden — samme serie forskjøvet én måned), to strømmer (`ant_soknader`, `ant_avslag`), utbetalt beløp og fire snittstørrelser. Én tolkningsregel er viktig nok til å stå her og ikke bare i kodeboken: `ant_soknader` teller husstander i terminbehandlingen *inkludert maskinell videreføring av løpende saker*. Det er saksvolum, ikke nye søknader, og skal omtales deretter.

*Form 2 — paneler* (`oslo_bydel`, `brukergruppe`, `nasjonalt_brukergruppe`, `framskriving_bydel`). Samme mål stablet i langformat. Bydelsnøkkelen er Husbankens kommunenummerering 0311–0325, der kommunenummer = 310 + bydelsnummer. Node 0301 er en nesten tom restkategori som i kilden bærer navnet «Oslo» — et navn som inviterer til å forveksle den med totalen. Den beholdes som egen node under merkelappen «Ufordelt (0301)» slik at hierarkiet summerer eksakt, og navnebyttet er dokumentert som vaskeinngrep V2. Bydelspanelet er ubalansert på nøyaktig ett punkt: 0301 er bare til stede i måneder der noden har minst én husstand. Det er verifisert (@tbl-kontroller) at aggregatet likevel treffer totalen eksakt, hvilket er den eneste egenskapen aggregering krever.

*Form 3 — statistikk-kuber i langformat* (SSB- og Oslo-statbanktabellene). Én rad er én celle, med kode-, navn- og verdikolonner. En kube er ikke en serie før alle dimensjoner unntatt tid er låst. Toromsleien i Oslo og Bærum krever for eksempel `Soner2_kode == "01"`, `AntRom_kode == "2"` og `ContentsCode_kode == "Husleie"` — tre filtre for én tallrekke. Uten full nedlåsing summerer man over en glemt dimensjon og får et tall som ser plausibelt ut. Det er kubefellen, og grunnen til at hver kube i denne analysen bare brukes gjennom navngitte filteroppskrifter, dokumentert i kodeboken.

*Form 4 — oppslagstabeller* (`intervensjoner`, `parametre`, `stromstotte`, `grunnbelop`, `arsrapport`, `forhandsanslag`). Én rad er én datert regelendring med mekanisme, forventet fortegn og verifiseringsstatus; en regelparameter; et månedsbeløp; et G-vedtak; et publisert nasjonalt årstall; et datert effektanslag for én intervensjon. Disse brukes aldri som serier. De *genererer* de deterministiske regressorene i @sec-met-x og forsyner regelmotorens konstanter.

@tbl-datasett plasserer alle objektene i denne inndelingen. Rad- og kolonnetall er telt programmatisk fra objektene, og oppslaget mot formtabellen er sikret med en konsistenssjekk: dersom datapakken en dag inneholder et objekt som ikke er klassifisert, stopper renderingen.

```{r}
#| label: tbl-datasett
#| tbl-cap: "Datasettene i analysen, gruppert etter form. «Én rad er» angir kornet — enheten én rad representerer — og er den eneste kuraterte kolonnen; rader og kolonner er telt fra objektene ved rendering. Tidsdekning står i @tbl-dekning."

korn <- tribble(
  ~objekt,                   ~form,        ~grain,
  "oslo",                    "1 serie",    "én måned (Oslo)",
  "nasjonalt",               "1 serie",    "én måned (Norge)",
  "oslo_bydel",              "2 panel",    "bydel × måned",
  "brukergruppe",            "2 panel",    "brukergruppe × måned (Oslo)",
  "nasjonalt_brukergruppe",  "2 panel",    "brukergruppe × måned (Norge)",
  "framskriving_bydel",      "2 panel",    "bydel × år (MMM, alle aldre)",
  "leiemarked",              "3 kube",     "prissone × rom × mål × år",
  "kpi_husleie",             "3 kube",     "konsumgruppe × mål × måned",
  "kpi_total",               "3 kube",     "mål × måned",
  "befolkning_oslo",         "3 kube",     "innholdsserie × kvartal (Oslo)",
  "aap_bydel",               "3 kube",     "bydel × kjønn × alder × år",
  "ufore_bydel",             "3 kube",     "bydel × kjønn × alder × år",
  "sosialhjelp_bydel",       "3 kube",     "bydel × kategori × år",
  "sosialhjelp_andel_bydel", "3 kube",     "bydel × alder × mål × år",
  "folkemengde_bydel_0819",  "3 kube",     "bydel × aldersgruppe × år",
  "folkemengde_bydel_1726",  "3 kube",     "bydel × aldersgruppe × år",
  "intervensjoner",          "4 oppslag",  "én datert regelendring",
  "parametre",               "4 oppslag",  "én regelparameter",
  "stromstotte",             "4 oppslag",  "én utbetalingsmåned",
  "grunnbelop",              "4 oppslag",  "ett G-vedtak (1. mai)",
  "arsrapport",              "4 oppslag",  "ett publisert årstall (nasjonalt)",
  "forhandsanslag",          "4 oppslag",  "ett effektanslag for én intervensjon"
)

# Kontrakt: hvert objekt i datapakken skal være klassifisert, og hver
# klassifisering skal peke på et objekt som finnes. Bryter det, stopper vi.
stopifnot(setequal(names(d), korn$objekt))

tibble(objekt = names(d),
       rader = map_int(d, nrow), kolonner = map_int(d, ncol)) |>
  left_join(korn, by = "objekt") |>
  arrange(form, objekt) |>
  select(Form = form, Objekt = objekt, `Én rad er` = grain,
         Rader = rader, Kol. = kolonner) |>
  kab(align = "lllrr")
```

### Dekning: hvor langt hver kilde rekker {#sec-met-dekning}

Dekning og vask er to forskjellige ting, og de blandes ofte. *Dekning* er en egenskap ved kilden: hvilken periode den beskriver, og med hvilken frekvens. Vi kan konstatere den, ikke endre den. *Vask* er noe vi gjør med dataene etter at de er hentet (@sec-met-vask). Å oppgi dekning i en tabell som heter «vask», slik en tidligere versjon av dette kapitlet gjorde, skjuler at det ene er observasjon og det andre er inngrep.

@tbl-dekning er beregnet fra filene, ikke skrevet av. Den siste kolonnen er den som styrer et modellvalg: avstanden fra hver kildes siste observasjon til utfallsseriens estimeringskant.

```{r}
#| label: tbl-dekning
#| tbl-cap: "Tidsdekning per datasett, avlest fra objektene ved rendering. «Til est.kant» er antall måneder fra kildens siste observasjon til utfallsseriens siste observasjon; negative tall betyr at kilden slutter før utfallsserien og dermed selv måtte prognostiseres for å brukes som prediktor."

est_kant <- max(oslo_est$dato)

spenn <- function(x) {
  if ("dato" %in% names(x) && inherits(x$dato, "Date")) {
    v <- x$dato[!is.na(x$dato)]
    steg <- if (n_distinct(x$dato) > 1) {
      median(as.numeric(diff(sort(unique(v)))))
    } else NA_real_
    frek <- dplyr::case_when(is.na(steg) ~ "—", steg <= 32 ~ "måned",
                             steg <= 95 ~ "kvartal", TRUE ~ "år")
    return(tibble(frekvens = frek, fra = format(min(v), "%Y-%m"),
                  til = format(max(v), "%Y-%m"), perioder = n_distinct(v),
                  slutt = max(v)))
  }
  if ("dato_utbetaling" %in% names(x)) {
    v <- x$dato_utbetaling
    return(tibble(frekvens = "måned", fra = format(min(v), "%Y-%m"),
                  til = format(max(v), "%Y-%m"), perioder = n_distinct(v),
                  slutt = max(v)))
  }
  if ("termin_fra" %in% names(x)) {
    v <- x$termin_fra[!is.na(x$termin_fra)]
    return(tibble(frekvens = "hendelse", fra = format(min(v), "%Y-%m"),
                  til = format(max(v), "%Y-%m"), perioder = nrow(x), slutt = max(v)))
  }
  if ("virkningsdato" %in% names(x)) {
    v <- x$virkningsdato
    return(tibble(frekvens = "år", fra = format(min(v), "%Y"),
                  til = format(max(v), "%Y"), perioder = n_distinct(v), slutt = max(v)))
  }
  if ("aar" %in% names(x)) {
    v <- x$aar[!is.na(x$aar)]
    return(tibble(frekvens = "år", fra = as.character(min(v)), til = as.character(max(v)),
                  perioder = n_distinct(v),
                  slutt = as.Date(sprintf("%d-12-01", max(v)))))
  }
  tibble(frekvens = "gjeldende", fra = "—", til = "—",
         perioder = nrow(x), slutt = as.Date(NA))
}

dekning <- map_dfr(d, spenn, .id = "objekt") |>
  left_join(korn |> select(objekt, form), by = "objekt") |>
  mutate(avstand = round(as.numeric(slutt - est_kant) / 30.44)) |>
  arrange(form, objekt)

dekning |>
  transmute(Form = form, Objekt = objekt, Frekvens = frekvens,
            Fra = fra, Til = til, Perioder = perioder,
            `Til est.kant` = if_else(is.na(avstand), "—",
                                     sprintf("%+d mnd", as.integer(avstand)))) |>
  kab(align = "llllrrr")
```

Tabellen gjør et forhold synlig som ellers blir stående som en påstand i løpende tekst. Alle de *stokastiske* kovariatene — husleieindeks, leienivåer, befolkningskvartaler, bydelsserier — slutter før utfallsseriens estimeringskant i `r est_til`. På horisontene 1–12 måneder måtte de derfor selv prognostiseres, slik at deres usikkerhet forplantet seg inn i prediksjonsintervallene uten dokumentert presisjonsgevinst. Det er den empiriske begrunnelsen for at hovedspesifikasjonen er *deterministisk informert*: den bruker bare regressorer som er perfekt kjent på alle horisonter — regelverkskalenderen og meldekortgridet. De stokastiske kovariatene reserveres for ML-referanseklassen, driftsdiagnostikken og bydelsratene (@sec-met-kovariater). Bare befolkningsframskrivingen dekker fremover, og den brukes utelukkende som nevner i bydelsanalysen, aldri som prediktor.

### Verifikasjon: regnskapsidentitetene {#sec-met-verifikasjon}

En administrativ totaltelling verifiseres mot sine egne regnskapsidentiteter. Identitetene er utsagn som *må* holde dersom uttrekket er korrekt, og som ikke holder ved et uhell: at to kalendre som er hentet med samme telleregel forskyver seg nøyaktig én måned, og at to uavhengige inndelinger av de samme husstandene summerer til det samme totalet.

@tbl-kontroller kjører kontrollene i selve dokumentet, og en kontroll som feiler stopper renderingen. Det er en reell egenskap, ikke en programerklæring: koden bak tabellen avslutter med et krav om at alle kriteriene er oppfylt. Bygger dokumentet, holder identitetene i den datapakken som ligger i repoet.

```{r}
#| label: tbl-kontroller
#| tbl-cap: "Verifikasjonskontroller, beregnet ved rendering. Kalenderidentiteten følger av etterskuddsutbetalingen (@eq-kalenderidentitet); summeringskontrollene er forutsetningen for hierarkisk avstemming i kapitlets andre del; prikkingskontrollen viser at personvernmaskeringen i kilden ikke kan ha truffet analysenivåene."

sisteM <- max(oslo_est$dato)

# K1 kalenderidentitet
idpar <- d$oslo |>
  transmute(dato_neste = dato %m+% months(1), termin = ant_husstander_termin) |>
  inner_join(d$oslo |> select(dato, utb = ant_husstander_utbetaling),
             by = c("dato_neste" = "dato")) |>
  filter(termin > 0, utb > 0)
k1_ok <- sum(idpar$termin == idpar$utb); k1_n <- nrow(idpar)

# K2/K3 summeringskonsistens
avvik <- function(panel) {
  panel |>
    filter(dato >= EST_START, dato <= sisteM) |>
    summarise(s = sum(ant_husstander_termin), .by = dato) |>
    left_join(oslo_est |> select(dato, M), by = "dato") |>
    summarise(mx = max(abs(s - M))) |> pull(mx)
}
k2 <- avvik(d$oslo_bydel); k3 <- avvik(d$brukergruppe)

# K4 komplett månedsrekke uten hull
grid_mnd <- seq(min(d$oslo$dato), max(d$oslo$dato), by = "month")
k4 <- sum(!grid_mnd %in% d$oslo$dato)

# K5 prikking. To tall: minste celle blant de femten bydelene og de fem
# brukergruppene, og maksimum for den ufordelte restnoden 0301 — som ligger
# i prikkingssonen og derfor må omtales for seg.
minste <- min(
  d$oslo_bydel |> filter(kommunenr != "0301", dato >= EST_START,
                         ant_husstander_termin > 0) |> pull(ant_husstander_termin),
  bg_est |> filter(M > 0) |> pull(M)
)
maks_0301 <- d$oslo_bydel |> filter(kommunenr == "0301") |>
  pull(ant_husstander_termin) |> max()

# K6 ingen manglende antallsverdier
k6 <- sum(is.na(d$oslo$ant_husstander_termin),
          is.na(d$oslo_bydel$ant_husstander_termin),
          is.na(d$brukergruppe$ant_husstander_termin))

# K7 saksidentiteten: behandlede saker = innvilget + avslag. Holder den, er
# ant_soknader en lineærkombinasjon og ikke selvstendig informasjon (@sec-met-observasjoner).
k7 <- d$oslo |> filter(ant_husstander_termin > 0) |>
  summarise(mx = max(abs(ant_soknader - ant_avslag - ant_husstander_termin))) |> pull(mx)

# K8 summeringskonsistens for ALLE mål, ikke bare antallet
maal <- c("ant_husstander_utbetaling", "ant_husstander_termin", "ant_soknader",
          "ant_avslag", "utbetalt_belop", "ant_over_tak")
avvik_maal <- function(panel) {
  max(map_dbl(maal, \(m) {
    panel |> summarise(s = sum(.data[[m]], na.rm = TRUE), .by = dato) |>
      inner_join(d$oslo |> select(dato, tot = all_of(m)), by = "dato") |>
      summarise(mx = max(abs(s - tot))) |> pull(mx)
  }))
}
k8 <- max(avvik_maal(d$oslo_bydel), avvik_maal(d$brukergruppe))

# K9 beløpsidentiteten: utbetalt beløp delt på antall skal gi snittkolonnen
k9 <- d$oslo |> filter(ant_husstander_utbetaling > 0, !is.na(gjsnitt_bostotte)) |>
  summarise(mx = max(abs(utbetalt_belop / ant_husstander_utbetaling - gjsnitt_bostotte))) |>
  pull(mx)

kontroller <- tribble(
  ~Kontroll, ~Krav, ~Resultat, ~ok,
  "K1  Kalenderidentitet: termin(m) = utbetaling(m+1)",
    "alle månedspar", sprintf("%s av %s par", nb(k1_ok), nb(k1_n)), k1_ok == k1_n,
  "K2  Bydelssum = Oslo-total, hver måned",
    "maks. avvik = 0", sprintf("%s husstander", nb(k2)), k2 == 0,
  "K3  Brukergruppesum = Oslo-total, hver måned",
    "maks. avvik = 0", sprintf("%s husstander", nb(k3)), k3 == 0,
  "K4  Sammenhengende månedsrekke",
    "ingen hull", sprintf("%s hull", nb(k4)), k4 == 0,
  "K5  Prikkingsgrense (kilden prikker celler < 4)",
    "minste celle ≥ 4 utenom restnoden", sprintf("minste celle: %s", nb(minste)), minste >= 4,
  "K6  Manglende antallsverdier",
    "ingen NA", sprintf("%s NA", nb(k6)), k6 == 0,
  "K7  Saksidentitet: søknader − avslag = mottakere",
    "avvik = 0", sprintf("%s husstander", nb(k7)), k7 == 0,
  "K8  Begge disaggregeringer summerer for alle seks mål",
    "maks avvik = 0", sprintf("%s", nb(k8)), k8 == 0,
  "K9  Beløpsidentitet: beløp / antall = snittkolonne",
    "avvik < 1 øre", sprintf("%s kr", nb(k9, 3)), k9 < 0.01
)

# Kontrakten dokumentet gir leseren: bygger PDF-en, holdt alle kontrollene.
if (!all(kontroller$ok)) {
  stop("Verifikasjon feilet: ",
       paste(kontroller$Kontroll[!kontroller$ok], collapse = "; "))
}

kontroller |>
  mutate(Status = if_else(ok, "OK", "AVVIK")) |>
  select(-ok) |>
  kab(align = "lllc")
```

Kontrollene sier fire ting. Begge kalendrene bærer identisk informasjon, slik @eq-kalenderidentitet krever, og forskyvningen kan derfor brukes uten informasjonstap. Hierarkiet og brukergruppesnittet summerer eksakt, hvilket er forutsetningen for avstemt prognose — og samtidig et bevis for at prikkingen ikke har truffet: en maskert celle ville brutt summen. Og minste observerte celle blant de femten bydelene og de fem brukergruppene er `r nb(minste)` husstander — to størrelsesordener over prikkingsgrensen på fire, så avstanden til maskeringssonen er ikke marginal. Ett unntak fortjener å nevnes framfor å skjules: den ufordelte restnoden 0301 ligger på 0–`r nb(maks_0301)` husstander og er dermed *inne i* prikkingssonen. Nettopp derfor er summeringskontrollen det bærende argumentet — hadde noden vært maskert i én måned, ville bydelssummen avveket fra totalen den måneden, og K2 ville feilet. Og K7 til K9 sier noe kontrollene i den forrige versjonen ikke fanget: summeringen holder for *alle seks* mål og ikke bare for antallet, beløpskolonnene er aritmetisk forenlige med hverandre — og søknadsserien er en lineærkombinasjon av de to andre. Det siste er ikke bare en bestått kontroll; det endrer hva serien kan brukes til, og behandles derfor som et funn i @sec-met-observasjoner.

### Ekstern validering: uttrekket mot publiserte tall {#sec-met-ekstern}

Regnskapsidentitetene i @sec-met-verifikasjon er *interne*: de viser at uttrekket er konsistent med seg selv. De ville også holdt om hele uttrekket var systematisk feil. Den sterkeste tilgjengelige kontrollen er derfor ekstern: å regne uttrekket opp mot tall Husbanken selv har publisert.

Årsrapportenes tabell 3.1 gir nasjonale årstall for antall husstander som mottok bostøtte i løpet av året og for utbetalt beløp [@husbanken2025aars]. Beløpet er direkte sammenliknbart — summen av månedlige utbetalinger over kalenderåret skal treffe det publiserte tallet. Antallet er det ikke: det publiserte tallet teller *unike* husstander gjennom året, mens uttrekket er en månedsbeholdning. Forholdet mellom de to er likevel informativt, fordi det måler gjennomstrømmingen, og en brå endring i det forholdet ville tydet på et brudd i telleregelen. @tbl-ekstern stiller de to opp mot hverandre.

```{r}
#| label: tbl-ekstern
#| tbl-cap: "Uttrekket mot Husbankens publiserte nasjonale årstall (årsrapport 2025, tabell 3.1). Beløp er direkte sammenliknbart. Antall er ikke: publisert tall er unike husstander gjennom året, uttrekket er månedsbeholdning — forholdet mellom dem måler gjennomstrømming."

ekstern <- d$nasjonalt |>
  filter(aar %in% d$arsrapport$aar) |>
  summarise(uttrekk_mill = sum(utbetalt_belop, na.rm = TRUE) / 1e6,
            snitt_mnd    = mean(ant_husstander_utbetaling),
            maks_mnd     = max(ant_husstander_utbetaling), .by = aar) |>
  inner_join(d$arsrapport, by = "aar") |>
  mutate(avvik_pst = 100 * (uttrekk_mill - utbetalt_bostotte_mill) / utbetalt_bostotte_mill,
         omlop     = husstander_unike_aaret / snitt_mnd) |>
  arrange(aar)

# Ekstern validering er bare en kontroll hvis den kan feile.
stopifnot(max(abs(ekstern$avvik_pst)) < 5,
          all(ekstern$maks_mnd <= ekstern$husstander_unike_aaret))

ekstern |>
  transmute(`År` = as.character(aar),
            `Publisert (mill. kr)` = nb(utbetalt_bostotte_mill),
            `Uttrekk (mill. kr)`   = nb(uttrekk_mill),
            `Avvik`                = pst(avvik_pst, 2),
            `Publiserte unike`     = nb(husstander_unike_aaret),
            `Snitt mnd.`           = nb(snitt_mnd),
            `Omløp`                = nb(omlop, 2)) |>
  kab(align = "lrrrrrr")
```

Valideringen gir to svar, og det ene av dem er en begrensning.

*Uttrekket treffer.* Avviket i utbetalt beløp er `r pst(max(abs(ekstern$avvik_pst)), 1)` eller mindre i alle fem år, og omløpstallet ligger stabilt mellom `r nb(min(ekstern$omlop), 2)` og `r nb(max(ekstern$omlop), 2)` — ingen endring i telleregelen har sneket seg inn i serien. Maks månedsbeholdning er lavere enn antall unike husstander i alle år, som den må være.

*Men uttrekket er omregnet rett, ikke utbetalt beløp.* Avviket er ensidig negativt fra 2022 og framover, mens `r ekstern$aar[which.min(abs(ekstern$avvik_pst))]` stemmer eksakt. Et systematisk underskudd på om lag én prosent er akkurat det etterkontrollen mot skatteoppgjøret produserer: statistikkbanken viser omregnet rett etter tilbakekreving, årsrapporten viser hva som faktisk ble utbetalt det året. Dette er den empiriske bekreftelsen på revisjonsmekanismen @sec-met-dv beskriver, og den har en konsekvens: beløpsserien modellerer *rett*, ikke kasse. For antallsserien — studiens utfall — er forskjellen uten betydning, siden et vedtak som senere omregnes i kroner fortsatt var et positivt vedtak.

### Vask: to inngrep, og de vi ikke gjør {#sec-met-vask}

Ordet «vask» stammer fra survey-data, der det betegner håndtering av frafall, målefeil, kodefeil, dubletter og vekting. Hver av disse eksisterer fordi et utvalg er *målt*, og målingen kan være feil. Her er situasjonen en annen: tallet er ikke en måling av et vedtak, det *er* antallet vedtak, telt i registeret som fatter dem. Det finnes ingen målefeil å vaske bort.

Det som finnes, er *representasjonsvalg* — måter kilden koder tilstander som ikke er tall: at en termin ennå ikke er kjørt, og at en restkategori bærer et misvisende navn. Vasken består derfor av nøyaktig to inngrep, begge i @tbl-vask, og det er ikke en påstand om at dataene var rene, men et fullstendig regnskap over hva som er gjort.

| Id | Inngrep | Omfang | Begrunnelse | Alternativet, og hva det ville kostet |
|:---|:-------------------|:----------|:----------------------|:-------------------------------------|
| **V1** | Sanntidskanten fjernes: siste månedsrad med `ant_husstander_termin = 0` | `r nrow(kant_rader)` rad (`r if (nrow(kant_rader)) format(kant_rader$dato, "%Y-%m") else "—"`) | Verdien er en strukturell null — terminkjøringen for måneden skjer først neste måned — ikke en observasjon av null mottakere | Å beholde raden legger et fall på 100 % inn i serien som data. Det ville forgiftet både sesongkomponent og trend, og gjort siste observasjon til det største bruddet i hele materialet |
| **V2** | Node 0301 gis merkelappen «Ufordelt (0301)» i stedet for kildens «Oslo» | 1 etikett, `r nb(sum(d$oslo_bydel$kommunenr == "0301"))` rader | Kilden gir restkategorien samme navn som totalen. Uendret inviterer det til å lese en node på 0–3 husstander som Oslo-tallet | Å beholde navnet er en ren felle for enhver som joiner på navn. Ingen tallverdi endres av byttet |

: Vaskeinngrepene, fullstendig. Begge er reversible og berører ingen tallverdi i utfallsserien. {#tbl-vask}

Like viktig er listen over det vi *ikke* gjør, og hvorfor, siden hvert av punktene er en beslutning og ikke en forglemmelse:

*Ingen imputering.* Snittstørrelsene (`gjsnitt_bostotte` og de tre andre) er tomme i måneder uten utbetalinger. I hele serien gjelder det `r nb(sum(is.na(oslo_termin$snitt)))` måned, og i estimeringsutvalget `r nb(sum(is.na(oslo_est$snitt)))`. De beholdes som manglende. Et imputert snitt er et konstruert tall som ville sett ut som en observasjon.

*Ingen uteliggerbehandling.* De største utslagene i serien er ikke feil — de er regelendringer, og de er hele poenget (@sec-met-observasjoner). En uteliggerrutine ville systematisk fjernet nettopp den variasjonen studien skal forklare.

*Ingen glatting og ingen forhåndssesongjustering.* Sesong- og kalendervariasjon modelleres eksplisitt (@eq-ssm, @eq-meldekort). Å justere før modellering ville skjult signalet i det leddet som skal bære det, og gjort prediksjonsintervallene misvisende.

*Ingen revisjon av historikk.* Serien revideres bakover i kilden ved etterkontroll mot skatteoppgjøret. Vi bruker uttrekket slik det forelå `r UTTREKKSDATO`, uten å forsøke å rekonstruere eldre vintages.

*Ingen enhetsharmonisering.* Alle antallsserier teller samme enhet — husstander — med samme telleregel på alle tre aggregeringsnivåer. Det er verifisert, ikke antatt (@tbl-kontroller).

Konsekvensen er verdt å si rett ut, fordi den er forutsetningen for at observasjonene i @sec-met-observasjoner betyr noe: hvert tall som presenteres nedenfor, er kildens eget tall. Det er ikke justert, glattet, vektet eller korrigert, og en leser som henter samme uttrekk fra statistikkbanken vil finne de samme verdiene.

### Estimeringsutvalget: hvorfor 2017 {#sec-met-utvalg}

Serien dekker `r serie_fra`–`r serie_til`. Estimeringsutvalget avgrenses til **januar 2017 og senere**, `r nb(n_est)` månedsobservasjoner (`r est_fra`–`r est_til`). Avgrensningen er ikke bekvemmelighet, men operasjonaliserer måleregimeargumentet i @sec-teori-beholdning: før 2017 ble inntektsgrunnlaget oppdatert årlig med ligningstall i juli, etter 2017 hentes månedsinntekt fortløpende fra a-ordningen. Serien før og etter skiftet er ikke samme datagenererende prosess.

Argumentet skal ikke hvile på sitatet alene, og gjør det ikke: signaturen er synlig i dataene.

```{r}
#| label: tbl-regime
#| tbl-cap: "Gjennomsnittlig nivå i juni–august relativt til eget årssnitt (= 1), etter måleregime. Juli-fallet før 2017 er ligningsoppdateringens artefakt; etter skiftet er det borte, mens juni-nivået preges av de regelverksbestemte mai- og junipunktene (G-regulering og prisjustering)."

oslo_termin |>
  mutate(aar = year(dato), mnd = month(dato),
         regime = if_else(aar <= 2016, "2010–2016  (ligning i juli)",
                                       "2017–2025  (a-ordningen)")) |>
  filter(aar <= 2025) |>
  group_by(regime, mnd) |>
  summarise(M = mean(M), .groups = "drop_last") |>
  mutate(rel = round(M / mean(M), 3)) |>
  filter(mnd %in% 6:8) |>
  select(regime, mnd, rel) |>
  pivot_wider(names_from = mnd, values_from = rel) |>
  kab(col.names = c("Måleregime", "Juni", "Juli", "August"), align = "lrrr")
```

Perioden 2010–2016 beholdes som kontekst og for robusthetsanalyse, men ingen modellparameter estimeres på tvers av regimeskiftet.

### Hva dataene viser {#sec-met-observasjoner}

Fem observasjoner bærer resten av kapitlet. Hver av dem har en konsekvens for modellvalget, og de står her nettopp fordi tallene er kildens egne (@sec-met-vask).

```{r}
#| label: tbl-deskriptiv
#| tbl-cap: "Deskriptiv statistikk for utfalls- og strømningsserier i estimeringsutvalget. Alle tall er antall husstander per termin."

oslo_est |>
  summarise(across(c(M, soknader, avslag),
                   list(min = min, median = median, mean = mean,
                        sd = sd, max = max))) |>
  pivot_longer(everything(), names_to = c("var", "stat"), names_sep = "_(?=[a-z]+$)") |>
  pivot_wider(names_from = stat, values_from = value) |>
  mutate(var = recode(var,
           M = "Mottakere (innvilget, termin)",
           soknader = "Søknader (terminbehandlede saker)",
           avslag = "Avslag"),
         across(where(is.numeric), round)) |>
  kab(col.names = c("Størrelse", "Min", "Median", "Snitt", "Std.avvik", "Maks"),
      align = "lrrrrr")
```

**Observasjon 1: nivået er høyt og persistensen sterk.** @tbl-deskriptiv gir tallene: rundt `r nb(M_snitt)` mottakere med et standardavvik på `r nb(M_sd)` — `r pst(M_cv)` av snittet. Det bekrefter beholdningsstrukturen i @eq-stockflow og forklarer hvorfor naive referanser er sterke motstandere: å gjenta forrige måned er en god prognose i en serie der bare inn- og utstrømmen kan flytte nivået. Konsekvens: enhver mer kompleks modell må slå sesongnaiv i ekte ut-av-utvalg-test, ikke i tilpasning (T1).

**Observasjon 2: av de tre seriene er bare to uavhengige — og den tredje leder ingenting.** Kontroll K7 i @tbl-kontroller viser at søknader minus avslag er *identisk lik* mottakertallet, i hver eneste måned og på hvert aggregeringsnivå. `ant_soknader` er altså ikke en observasjon ved siden av utfallet, men summen av innvilgede og avslåtte saker i terminkjøringen — en definisjonsmessig identitet, ikke data. Av de tre seriene i @tbl-deskriptiv er det derfor bare to frihetsgrader, og den ekstra informasjonen ligger i avslagene alene.

Avslagene er heller ikke ledende. Avslagsandelen samvarierer negativt med endringen i mottakertallet i *samme* måned og positivt i den neste — mekanisk tilbakeslag — men korrelasjonen forsvinner ved to måneders lag. En serie som først røper seg samtidig med utfallet, kan ikke varsle det. Konsekvens: verken søknader eller avslag inngår som prediktorer, og begrunnelsen er nå empirisk framfor en henvisning til at de ligger i samme kalender. Det er samtidig en presisering av kapittel 2: strømmene $I_t$ og $U_t$ i @eq-stockflow er ikke observert i dette datagrunnlaget, og lar seg ikke rekonstruere fra søknads- og avslagskolonnene.

**Observasjon 3: de store bevegelsene er daterte regelendringer, ikke trend.** @tbl-hendelser lister de seks største månedsendringene i estimeringsutvalget og slår dem opp mot den kuraterte regelverkskalenderen.

```{r}
#| label: tbl-hendelser
#| tbl-cap: "De seks største månedsendringene i estimeringsutvalget, slått opp mot intervensjonstabellen. Oppslaget er automatisk: en endring knyttes til en hendelse dersom hendelsen starter i samme termin, eller dersom terminen er den første etter at et vindu er lukket."

start <- d$intervensjoner |> filter(!is.na(termin_fra)) |>
  transmute(dato = termin_fra, merke = id)
slutt <- d$intervensjoner |> filter(!is.na(termin_til)) |>
  transmute(dato = termin_til %m+% months(1), merke = paste0(id, " slutt"))

hendelser <- oslo_est |>
  mutate(endring = M - lag(M), pst = 100 * (M / lag(M) - 1)) |>
  slice_max(abs(endring), n = 6) |>
  left_join(bind_rows(start, slutt) |> summarise(merker = paste(merke, collapse = ", "),
                                                 .by = dato),
            by = "dato") |>
  arrange(dato)

hendelser |>
  transmute(Termin = format(dato, "%Y-%m"),
            Mottakere = nb(M), Endring = nb(endring),
            `Endring (%)` = nb(pst, 1),
            Regelhendelse = coalesce(merker, "ingen")) |>
  kab(align = "lrrrl")
```

Av de seks endringene faller `r sum(!is.na(hendelser$merker))` sammen med en datert regelendring. Den sjette, `r hendelser |> filter(is.na(merker)) |> pull(dato) |> format("%Y-%m") |> paste(collapse = ", ")`, gjør det ikke — og den er ikke et mysterium, men det neste funnet. Konsekvens: bruddene er kjente og daterte på forhånd, hvilket er selve forutsetningen for intervensjonsanalyse framfor uteliggerdeteksjon (@sec-met-x).

**Observasjon 4: den nest største systematiske bevegelsen er administrativ, ikke økonomisk.** Utenom regelendringene svinger serien måned til måned med et gjennomsnittlig absoluttavvik på `r pst(vol_oslo[["for_"]])` fram til desember 2024 — en sagtann på syv til ti prosent som verken er trend eller normal sesong. Det er meldekortsyklusen: i måneder med tre 14-dagersutbetalinger ble mottakere med dagpenger og AAP skjøvet over inntektsgrensen (@eq-meldekort). Konsekvens: en kalenderregressor er ikke en finpuss, men nødvendig for at sesongkomponenten ikke skal absorbere en deterministisk, kjent effekt.

**Observasjon 5: mekanismetesten slår ut — i riktig gruppe.** Skjermingen fra januar 2025 medregner bare to tredeler av meldekortytelsen i tre-utbetalingsmåneder, og skal dermed lukke kanalen fra observasjon 4. @fig-vol viser gjennomsnittlig absolutt månedsendring per brukergruppe før og etter. Behandlingsgruppen faller med faktor `r nb(vol_beh$faktor, 1)`; de øvrige gruppene faller med faktor `r nb(min(vol_andre$faktor), 1)` til `r nb(max(vol_andre$faktor), 1)`.

```{r}
#| label: fig-vol
#| fig-cap: "Gjennomsnittlig absolutt månedsendring i mottakertallet, per brukergruppe i Oslo. Fylt punkt er 2017–2024, åpent punkt 2025–2026; strekket er fallet. Behandlingsgruppen for skjermingen — husstander med midlertidige trygdeytelser — er markert i mørk farge, de øvrige fire er kontroller i mekanismedimensjonen. Fallet er av en helt annen størrelsesorden i gruppen mekanismen peker på."
#| fig-width: 6.4
#| fig-height: 2.9

# Formvalg: hantelfigur (dumbbell), ikke helningsfigur. Med fem grupper og
# tette verdier kolliderer etikettene i en helningsfigur; her bærer y-aksen
# identiteten, og avstanden mellom punktene er selve budskapet.
vol_fig <- vol_bg |>
  mutate(gruppe = recode(brukergruppe,
           "Husstander med midlertidige trygdeytelser" = "Midlertidige trygdeytelser",
           "Husstander uten trygdeytelser"             = "Uten trygdeytelser"),
         beh = brukergruppe == BEH) |>
  arrange(for_) |>
  mutate(gruppe = fct_inorder(gruppe))

ggplot(vol_fig, aes(y = gruppe)) +
  geom_segment(aes(x = etter, xend = for_, colour = beh), linewidth = 1.1) +
  geom_point(aes(x = for_, colour = beh), size = 2.4) +
  geom_point(aes(x = etter), colour = "white", size = 2.4) +
  geom_point(aes(x = etter, colour = beh), size = 2.4, shape = 21, stroke = 0.9) +
  geom_text(aes(x = for_, label = nb(for_, 1), colour = beh),
            hjust = -0.55, size = 2.5) +
  geom_text(aes(x = etter, label = nb(etter, 1), colour = beh),
            hjust = 1.55, size = 2.5) +
  scale_colour_manual(values = c(`FALSE` = "grey58", `TRUE` = "#00505c")) +
  scale_x_continuous(labels = \(x) paste0(nb(x), " %"),
                     limits = c(0, NA), expand = expansion(c(0.06, 0.12))) +
  labs(x = "Snitt |endring| per måned", y = NULL) +
  theme_minimal(base_size = 9) +
  theme(legend.position = "none",
        panel.grid.major.y = element_blank(),
        panel.grid.minor = element_blank(),
        panel.grid.major.x = element_line(linewidth = 0.25, colour = "grey88"),
        axis.title.x = element_text(size = 8, colour = "grey35"),
        axis.text.y = element_text(colour = c(rep("grey25", 4), "grey25")))
```

Dette er så nær et kontrollert eksperiment materialet kommer. Regelendringen traff hele landet samtidig, så det finnes ingen kontrollkommune (@sec-met-ideal) — men den traff bare én av fem brukergrupper, og de fire andre fungerer som kontroller. Konsekvens: T3 er ikke en retningsprediksjon, men en skarp nullhypotese med en gruppe der effekten skal være der og fire der den ikke skal være.

@fig-serie viser hele serien med regimeskillet og de daterte regelendringene markert.

```{r}
#| label: fig-serie
#| fig-cap: "Husstander med innvilget bostøtte i Oslo, terminkalender. Grått felt: perioden utenfor estimeringsutvalget, før måleregimeskiftet i 2017. Stiplede linjer med id-koder er daterte regelendringer; se @tbl-intervensjoner for form og forventet fortegn. Y-aksen er ikke nullpunktforankret — bevegelsene som beskrives, er på 5–25 prosent av nivået og ville vært uleselige mot en akse fra null."
#| fig-width: 6.6
#| fig-height: 3.0

nokkel <- d$intervensjoner |>
  filter(id %in% c("I01", "I05", "I12", "I14", "I17")) |>
  transmute(id, dato = termin_fra) |>
  arrange(dato) |>
  # Alternerende etiketthøyde: I12, I14 og I17 ligger for tett til å stå på
  # samme linje uten å overlappe.
  mutate(hoyde = max(oslo_termin$M) * c(1.075, 1.005, 1.075, 1.005, 1.075)[row_number()])

ggplot(oslo_termin, aes(dato, M)) +
  annotate("rect", xmin = min(oslo_termin$dato), xmax = EST_START,
           ymin = -Inf, ymax = Inf, fill = "grey93") +
  annotate("text", x = as.Date("2013-06-01"), y = min(oslo_termin$M),
           label = "utenfor estimeringsutvalget", vjust = -0.6,
           size = 2.5, colour = "grey45") +
  geom_vline(data = nokkel, aes(xintercept = dato),
             linetype = "22", colour = "grey55", linewidth = 0.35) +
  geom_line(linewidth = 0.5, colour = "#00505c") +
  geom_text(data = nokkel, aes(x = dato, y = hoyde, label = id),
            hjust = -0.18, size = 2.5, colour = "grey30") +
  scale_y_continuous(labels = \(x) nb(x),
                     expand = expansion(c(0.04, 0.12))) +
  scale_x_date(date_breaks = "2 years", date_labels = "%Y",
               expand = expansion(0.015)) +
  labs(x = NULL, y = "Husstander (termin)") +
  theme_minimal(base_size = 9) +
  theme(panel.grid.minor = element_blank(),
        panel.grid.major = element_line(linewidth = 0.25, colour = "grey88"),
        axis.title.y = element_text(size = 8, colour = "grey35"))
```

Figuren viser regimeforskjellen direkte: serien før 2017 ligger systematisk høyere og bærer et regelmessig juli-fall som forsvinner etter skiftet (@tbl-regime). Toppunktet er termin `r format(topp$dato, "%Y-%m")` med `r nb(topp$M)` husstander, umiddelbart før vinduslukkingen I12 tok `r pst(abs(apr24_pst))` av bestanden på én termin — seriens største enkeltbevegelse, og et brudd som er datert på forhånd i regelverkskalenderen.

## Estimand, kalendre og informasjonsmengde {#sec-met-estimand}

Estimanden (@eq-estimand) er den prediktive fordelingen for utfallsserien definert i @sec-met-dv:

$$p\left(M^{T}_{\tau+h} \,\middle|\, \mathcal{I}_{\tau}\right), \qquad h = 1, \dots, 12,$$ {#eq-estimand}

med punktprognose (medianen) og 80- og 95-prosents prediksjonsintervaller hvis empiriske dekning er en del av estimanden selv, jf. forskningsspørsmålet. Analysen føres i terminkalenderen; utbetalingskalenderen nås ved forskyvningen i @eq-kalenderidentitet.

Informasjonsmengden $\mathcal{I}_{\tau}$ er definert av produksjonssyklusen. Terminkjøringen for måned $m$ skjer først i påfølgende måned — per `r UTTREKKSDATO` forelå `r est_til`-terminen, ikke juli-terminen — slik at en prognoseopprinnelse lagt til månedsskiftet $\tau$ har terminserien til og med $\tau - 1$, hele regelverkskalenderen med vedtatte men ennå ikke ikrafttrådte endringer, og NAVs forhåndspubliserte utbetalingskalender [@navUtbetaling]. Denne vintage-disiplinen håndhever at ingen modell ser informasjon den ikke ville hatt i drift. At serien i tillegg revideres bakover gjennom etterkontroll mot skatteoppgjøret [@husbanken9c22], behandles som robusthetsspørsmål og ikke som del av hovedprotokollen, siden historiske vintages ikke er offentlig tilgjengelige (@sec-met-ideal).

## Regelverksinformasjon som design: intervensjonsmatrisen {#sec-met-x}

Intervensjonsmatrisen oversetter den primærkildebelagte regelverkskalenderen — kuratert i `intervensjonstabell.csv` med kilde og verifiseringsstatus per hendelse — til deterministiske regressorer i terminkalenderen. Hver hendelse tilordnes en funksjonsform fra @eq-transfer etter sin institusjonelle mekanikk, med fortegn og form som *prediksjoner* som kan felles empirisk, jf. asymmetriproposisjonen i @sec-teori-beholdning:

| Regressor | Termindatering | Form | Mekanisme og forventning |
|:---------------|:-----------------|:-----------------|:----------------------------------|
| `win_covid` | 2020m4–2020m10 | vindu (trinn inn/ut) | Midlertidige koronaregler; positivt nivåskift [@husbanken2021aars] |
| `win_strom` | 2021m12–2024m3 | vindu | Redusert progressivt egenandelsledd (0,28 → 0,12 %) løftet den implisitte inntektsgrensen $\tilde y^{*}$; om lag 25 000 husstander nasjonalt inn — og ut igjen ved avvikling. Inn-kanten forventes gradvis (søknadsbasert, $\delta > 0$), ut-kanten skarp (automatisk omregning, $\delta = 0$) [@husbanken2023aars; @husbanken2024aars] |
| `pakke_2024h2` | 2024m7/m9/m10 | samlet trinn | Boutgiftstak +4 000 kr (juli), oppvarmingstillegg til alle (september) og 18–19-åringers inntekt ut (oktober) ligger to til tre måneder fra hverandre og kan ikke identifiseres enkeltvis i en serie av denne lengden (@sec-met-ideal); de estimeres som én pakke med felles positivt fortegn, alternativt med de tre trinnene som kandidater i spike-and-slab-klassen [@husbanken2024aars; @husbanken2024rnb] |
| `step_skjerm` | 2025m1– | trinn (kandidat) | AAP- og dagpengeskjermingen virker primært gjennom kalenderregressoren (@sec-met-kalender); et eventuelt residualt nivåløft testes som kandidat, ikke pålegges [@kdd2025; @husbanken2025aars] |
| `strom_belop` | 2021m2–2024m3 | intensitet (kr) | Ekstrautbetalingenes kronebeløp per husstand, forskjøvet til termin; kun i beløpsmodeller — engangsutbetalinger endret ikke berettigelse |

: Intervensjonsmatrisens kolonner med funksjonsform og teoretisk forventning. {#tbl-intervensjoner}

```{r}
#| label: met-intervensjoner
mnd_seq <- tibble(dato = seq(as.Date("2010-01-01"), as.Date("2027-12-01"), by = "month"))

X <- mnd_seq |>
  mutate(
    win_covid    = as.integer(dato >= as.Date("2020-04-01") & dato <= as.Date("2020-10-01")),
    win_strom    = as.integer(dato >= as.Date("2021-12-01") & dato <= as.Date("2024-03-01")),
    step_tak     = as.integer(dato >= as.Date("2024-07-01")),
    step_oppv    = as.integer(dato >= as.Date("2024-09-01")),
    step_1819    = as.integer(dato >= as.Date("2024-10-01")),
    pakke_2024h2 = (step_tak + step_oppv + step_1819) / 3,
    step_skjerm  = as.integer(dato >= as.Date("2025-01-01"))
  )

# Kronekanalen: strømbeløp per husstand, utbetalingsmåned -> termin.
# Oppslagstabellen har én rad per utbetalingsmåned; sjekken sikrer at joinen
# ikke kan duplisere rader i X dersom kilden en dag får flere rader per måned.
strom_termin <- d$stromstotte |>
  transmute(dato = dato_utbetaling %m-% months(1), strom_belop = belop_husstand)
stopifnot(!any(duplicated(strom_termin$dato)))

X <- X |>
  left_join(strom_termin, by = "dato") |>
  mutate(strom_belop = replace_na(strom_belop, 0))
stopifnot(nrow(X) == nrow(mnd_seq))
```

Pakkeregressoren `pakke_2024h2` er gjennomsnittet av de tre enkelttrinnene og trapper dermed opp i tre steg (1/3, 2/3, 1) med full effekt fra termin oktober 2024; enkelttrinnene beholdes i matrisen utelukkende som kandidater for automatisk regressorvalg. To deterministiske *gjentakende* regelverkspunkter holdes utenfor med vilje: grunnbeløpsreguleringen (1. mai) og prisjusteringen av forskriftssatsene (1. juni, dokumenterte prosenter 2023–2025). Begge er årvisse og absorberes i utgangspunktet av sesongkomponenten i @eq-ssm; de reaktiveres som eksplisitte mai- og juniregressorer bare dersom residualdiagnostikken i del 2 krever det — et valg som holder antall frie parametre nede i en serie med `r nb(n_est)` observasjoner.

## Kalenderregressoren: konstruksjon og fasekalibrering {#sec-met-kalender}

Meldekortytelser utbetales per 14. dag i nasjonalt synkroniserte perioder [@kdd2025]. Kalendermåned $t$ inneholder dermed $u_t \in \{2, 3\}$ utbetalinger, med 26 utbetalinger per år. Grid-strukturen er kjent; det eneste frie er *fasen* — hvilken av de fjorten mulige forskyvningene den nasjonale syklusen følger. I stedet for å importere fasen fra en ekstern kalender kalibreres den fra dataene selv, med NAVs publiserte datoer som driftskilde fremover [@navUtbetaling]:

$$u_t(\varphi) = \#\{k : a_\varphi + 14k \in t\}, \qquad a_\varphi = a_0 + \varphi, \quad \varphi \in \{0, \dots, 13\}$$ {#eq-fase}

Kalibreringen bruker behandlingsgruppens egen terminserie — husstander med midlertidige trygdeytelser, nasjonalt — der mekanismen må ligge hvis den finnes. La $z_t$ være seriens relative avvik fra en sentrert $2\times12$-MA-trend i førperioden (2017–2024, koronavinduet ekskludert). Fasen velges som

$$\hat\varphi = \arg\min_{\varphi} \; t\text{-verdi}\Big(\bar z_{\,u(\varphi)=3} - \bar z_{\,u(\varphi)=2}\Big)$$ {#eq-kalibrering}

```{r}
#| label: tbl-kalender
#| tbl-cap: "Fasekalibrering av meldekortgridet: de tre fasene med sterkest utslag. Vinnerfasen gir det største negative gapet mellom tre- og to-utbetalingsmåneder; nest best er ukes-aliaset (± 7 dager) av samme signal. t-verdiene er kalibreringsstatistikk beregnet på MA-detrendede, seriekorrelerte avvik, og tolkes ikke som formelle tester."

u_av_fase <- function(phi, fra = as.Date("2010-01-01"), til = as.Date("2027-12-31")) {
  anker <- as.Date("2014-01-06") + phi   # vilkårlig mandag som origo for fasen
  # Rygg tilbake i hele 14-dagerssteg til før `fra`, slik at gridet dekker hele
  # perioden uansett startår. Fasen er uendret av dette; bare utstrekningen.
  start  <- anker - 14 * ceiling(as.numeric(anker - fra) / 14)
  datoer <- seq(start, til, by = "14 days")
  tibble(dato = floor_date(datoer[datoer >= fra], "month")) |> count(dato, name = "u")
}

w   <- c(0.5, rep(1, 11), 0.5) / 12
beh <- d$nasjonalt_brukergruppe |>
  filter(brukergruppe == BEH) |>
  transmute(dato, M = ant_husstander_termin) |>
  filter(M > 0) |>
  arrange(dato) |>
  mutate(MA = as.numeric(stats::filter(M, w, sides = 2)), z = M / MA - 1) |>
  filter(!is.na(z),
         dato >= as.Date("2017-01-01"), dato <= as.Date("2024-12-01"),
         !(dato >= as.Date("2020-03-01") & dato <= as.Date("2020-12-01")))

kalibrering <- map_dfr(0:13, function(phi) {
  beh |>
    inner_join(u_av_fase(phi), by = "dato") |>
    summarise(fase = phi,
              gap  = mean(z[u == 3]) - mean(z[u == 2]),
              t    = gap / sqrt(var(z[u == 3]) / sum(u == 3) +
                                var(z[u == 2]) / sum(u == 2)))
})
fase_valgt <- kalibrering |> slice_min(t, n = 1)

kalibrering |>
  arrange(t) |> head(3) |>
  transmute(`Fase (dager)` = fase, `Gap (%)` = nb(100 * gap, 1), `t-verdi` = nb(t, 1)) |>
  kab(align = "rrr")
```

Kalibreringen i @tbl-kalender peker entydig på én fase, og størrelsesordenen fortjener en setning: i månedene med tre meldekortutbetalinger lå behandlingsgruppens mottakertall `r pst(abs(100 * fase_valgt$gap))` under egen trend. Det administrativt skapte sesongfallet som skjermingen fra 2025 skulle fjerne [@kdd2025], er altså ikke en fotnote i serien, men dens nest største systematiske bevegelse etter regelendringene selv — helt i tråd med observasjon 4 i @sec-met-observasjoner. Regressoren defineres i tråd med @eq-meldekort, splittet ved regimeskiftet:

```{r}
#| label: met-kalender-reg
u_serie <- u_av_fase(fase_valgt$fase)
X <- X |>
  left_join(u_serie, by = "dato") |>
  mutate(
    k_pre  = (u - 26 / 12) * as.integer(dato <= as.Date("2024-12-01")),
    k_post = (u - 26 / 12) * as.integer(dato >= as.Date("2025-01-01"))
  )
stopifnot(!anyNA(X$u), all(X$u %in% c(2, 3)))
```

Skjermingens mekanikk skjerper teoriens prediksjon fra «nær null» til et punktnullpunkt: når bare to tredeler av ytelsen medregnes i tre-utbetalingsmåneder, blir telt inntekt $3 \times \tfrac{2}{3} = 2$ utbetalinger — *identisk med en normalmåned*. Gitt korrekt administrasjon er kalendereffekten mekanisk eksakt nøytralisert, slik at $\beta_{k,\text{post}} = 0$ er en skarp nullhypotese, ikke en approksimasjon. Konstruksjonen har dessuten en uavhengig kontroll utenfor kalibreringsvinduet, dokumentert i @fig-vol: behandlingsgruppens gjennomsnittlige absolutte månedsendring i Oslo faller med faktor `r nb(vol_beh$faktor, 1)` fra 2025, mot `r nb(min(vol_andre$faktor), 1)`–`r nb(max(vol_andre$faktor), 1)` for de øvrige gruppene. Før innlevering forankres den kalibrerte fasen i tillegg eksternt mot NAVs publiserte meldekortdatoer.

## Analysesett, transformasjon og hierarki {#sec-met-kovariater}

Kovariatkonstruksjonen følger rolledelingen begrunnet i @sec-met-dekning: husleieindeksen (04.1), leienivåene og den interpolerte befolkningsserien bygges som månedsserier, men går kun til ML-referanseklassen, driftsdiagnostikken og bydelsratene — aldri inn i hovedspesifikasjonen.

```{r}
#| label: met-analysesett
kpi_husleie <- d$kpi_husleie |>
  filter(Konsumgrp_kode == "04.1", ContentsCode_kode == "KpiIndMnd") |>
  transmute(dato, kpi_husleie = verdi)

bef_mnd <- mnd_seq |>
  left_join(d$befolkning_oslo |>
              filter(ContentsCode_kode == "Folketallet1") |>
              transmute(dato, N = verdi),
            by = "dato") |>
  mutate(N = approx(as.numeric(dato[!is.na(N)]), N[!is.na(N)],
                    xout = as.numeric(dato), rule = 2)$y)

analyse <- oslo_est |>
  left_join(X, by = "dato") |>
  left_join(kpi_husleie, by = "dato") |>
  left_join(bef_mnd, by = "dato") |>
  mutate(log_M = log(M))

# Analysesettet skal ha nøyaktig én rad per termin i estimeringsutvalget, og de
# deterministiske regressorene skal være komplette. Kravet er utledet av
# utvalget, ikke skrevet som et fast tall, slik at neste datavintage ikke
# bryter renderingen uten grunn.
stopifnot(nrow(analyse) == n_est,
          !anyNA(analyse$u), !anyNA(analyse$k_pre), !anyNA(analyse$k_post))

# Hierarkiet, klargjort for MinT-avstemming i del 2.
bydel_termin <- d$oslo_bydel |>
  mutate(bydel = if_else(kommunenr == "0301", "Ufordelt (0301)", bydel)) |>
  transmute(kommunenr, bydel, dato, M = ant_husstander_termin) |>
  filter(dato >= EST_START, dato <= max(oslo_est$dato))

bg_termin <- bg_est
```

Modelleringen skjer på logskala, $y_t = \ln M^{T}_t$: nivåserien er strengt positiv, regelendringenes effekter er multiplikative i @eq-takeup sin dekomponering, og logtransformasjonen gjør intervallene asymmetriske på nivåskala der de skal være det. Populasjonsoffset brukes ikke i hovedspesifikasjonen — Oslos befolkning beveger seg under én prosent på tolv måneder; rater reserveres for bydelsanalysen og driftsdiagnostikken. Analysesettet er dermed `r nb(nrow(analyse))` terminer × `r nb(ncol(analyse))` kolonner der alt foran er destillert: utfallet fra form 1, regressorene generert fra form 4, kalendertrioen fra det kalibrerte gridet, kovariatene fra form 3 låst ned til én kolonne hver. Hierarkiet — Oslo, femten bydeler og ufordelt-noden, med brukergruppene som alternativt snitt — står summeringskonsistent klart for MinT-avstemming i del 2 [@wickramasuriya2019].


## Seriediagnostikk: hva serien er, før den modelleres {#sec-met-diagnostikk}

Diagnostikken hører logisk sett tidlig, men den kan ikke kjøres tidligere enn her, og grunnen er verdt en setning. En enhetsrottest på den rå serien svarer på om det finnes en stokastisk trend. Med fem daterte nivåskift i utvalget er det spørsmålet nesten uinformativt: strukturelle brudd gjør at en stasjonær serie ser integrert ut, og enhetsrottester er systematisk skjeve mot ikke å forkaste under slike brudd [@perron1989]. Kjørt på rådata ville testen bare bekreftet at det finnes brudd — noe regelverkskalenderen allerede sa. Den informative testen er den *betingede*: er serien stasjonær gitt de deterministiske leddene? Den forutsetter at intervensjonsmatrisen (@sec-met-x) og kalenderregressoren (@sec-met-kalender) allerede er bygget, og hører derfor hjemme nettopp her.

```{r}
#| label: tbl-diagnostikk
#| tbl-cap: 'Seriediagnostikk før og etter at de deterministiske leddene er trukket ut. «Betinget» er OLS-residualen fra en regresjon av logserien på intervensjonsmatrisen og kalenderregressoren. KPSS tester nullhypotesen om stasjonaritet: lav teststatistikk og høy p-verdi betyr at nullhypotesen står.'

ts_diag <- analyse |> mutate(mnd = yearmonth(dato)) |> as_tsibble(index = mnd)
det <- lm(log_M ~ win_covid + win_strom + pakke_2024h2 + k_pre + k_post, data = ts_diag)
tr_diag <- tibble(mnd = ts_diag$mnd, e = as.numeric(residuals(det))) |> as_tsibble(index = mnd)

hent <- function(x, v) {
  u <- x |> features({{ v }}, list(kpss = unitroot_kpss, nd = unitroot_ndiffs, nsd = unitroot_nsdiffs))
  st <- x |> features({{ v }}, feat_stl)
  tibble(kpss = u$kpss_kpss_stat, p = u$kpss_kpss_pvalue, nd = u$nd_ndiffs, nsd = u$nsd_nsdiffs,
         trend = st$trend_strength, sesong = st$seasonal_strength_year)
}
diag_tab <- bind_rows(hent(ts_diag, log_M) |> mutate(serie = "Rå logserie"),
                      hent(tr_diag,  e)     |> mutate(serie = "Betinget på deterministiske ledd"))

diag_tab |>
  transmute(Serie = serie,
            `KPSS` = nb(kpss, 2),
            `p-verdi` = if_else(p <= 0.01, "≤ 0,01", if_else(p >= 0.1, "≥ 0,10", nb(p, 2))),
            `Differensiering` = nd, `Sesongdiff.` = nsd,
            `Trendstyrke` = nb(trend, 2), `Sesongstyrke` = nb(sesong, 2)) |>
  kab(align = "lrrrrrr")
```

@tbl-diagnostikk gir studiens kanskje reneste enkeltfunn. Den rå logserien forkaster stasjonaritet og krever én differensiering. Trekker man ut de fem deterministiske leddene, står nullhypotesen om stasjonaritet, og ingen differensiering er nødvendig. **Ikke-stasjonariteten i serien er regelendringene.** De samme fem leddene forklarer `r pst(100 * summary(det)$r.squared)` av variansen i $\ln M^T_t$ alene.

Funnet er ikke bare et diagnostisk kuriosum: det bekreftes uavhengig av modellvalget i @sec-met-modeller. Referansemodellen uten regressorer velger $d = 1$ — den må differensiere bruddene bort. Spesifikasjonene med intervensjonsmatrisen velger $d = 0$. To ulike prosedyrer, samme konklusjon, og det er den beste formen for bekreftelse en diagnostikk kan få.

## Modellspesifikasjoner {#sec-met-modeller}

Modellvalget følger hierarkiet i @sec-teori-modeller, men med én tilleggsbetingelse som kommer fra forskningsspørsmålet selv. Spørsmålet er ikke bare *hvor presist* serien kan prognostiseres, men *hvor mye det hjelper* å modellere kjente regelendringer eksplisitt. Et slikt spørsmål besvares ikke av én god modell mot én dårlig; det besvares av en **nøstet stige** der modellene skiller seg fra hverandre i nøyaktig én informasjonskilde av gangen. @tbl-modeller er bygget slik.

| Id | Modell | Informasjon utover $y_t$ | Rolle i designet |
|:-----|:-------------------|:---------------------|:--------------------------|
| **M0** | Naiv (tilfeldig gang) | ingen | nedre grense; @eq-stockflow gjør denne sterk |
| **M1** | Sesongnaiv | 12-måneders lag | referanse og nevner i @eq-mase |
| **M2** | ETS, automatisk klasse | ingen | etablert referanse [@hyndman2002] |
| **M3** | SARIMA, automatisk orden | ingen | etablert referanse [@box2015]; utgangspunktet stigen måles mot |
| **M4** | RegARIMA $+\ k$ | kalenderregressoren | isolerer kalenderbidraget |
| **M5** | RegARIMA $+\ X$ | intervensjonsmatrisen | isolerer regelverksbidraget |
| **M6** | RegARIMA $+\ X + k$ | begge | **hovedspesifikasjon** |
| **M7** | M6 $+$ pålagt effekt | daterte forhåndsanslag | svarer **UB**: regelendring vedtatt, men ikke trådt i kraft |

: Modellstigen. Hvert trinn legger til nøyaktig én informasjonskilde, slik at differansen mellom to modeller kan tilskrives den kilden og ikke et modellbytte. {#tbl-modeller}

Stigen gjør tre sammenlikninger til kontrollerte eksperimenter. M6 mot M3 er den samlede gevinsten av deterministisk informasjon — hovedspørsmålets kjerne. M6 mot M4 isolerer regelverkets bidrag, M6 mot M5 kalenderens. Uten stigen kunne en gevinst like gjerne skyldtes at RegARIMA tilfeldigvis passer serien bedre enn SARIMA.

**Hovedklassen.** Innenfor hovedklassen estimeres RegARIMA-varianten: ARIMA-feilledd med deterministiske regressorer,

$$\ln M^{T}_{t} \;=\; \beta' x_t \;+\; \eta_t,
\qquad \phi_p(L)\,\Phi_P(L^{12})\,(1-L)^{d}(1-L^{12})^{D}\,\eta_t \;=\; \theta_q(L)\,\Theta_Q(L^{12})\,\varepsilon_t$$ {#eq-regarima}

der $x_t$ er kolonnene fra @sec-met-x og @sec-met-kalender. Dette er standardarkitekturen i offisiell statistikk for nettopp denne problemklassen [@findley1998]. Den strukturelle tilstandsromsvarianten av samme dekomponering (@eq-ssm) er utsatt til BSTS-utvidelsen, der den hører sammen med spike-and-slab-utvelgelsen av kandidatregressorer [@scott2014] — å estimere den her uten den utvelgelsen ville gitt samme dekomponering med færre verktøy.

**Hovedspesifikasjonens regressorsett.** $X$ inneholder `win_covid`, `win_strom` og `pakke_2024h2`; $k$ inneholder `k_pre` og `k_post`. Tre valg fortjener begrunnelse. Enkelttrinnene `step_tak`, `step_oppv` og `step_1819` holdes ute fordi de er kollineære på månedsdata (@sec-met-ideal) og hører hjemme i spike-and-slab-klassen. `step_skjerm` holdes ute av hovedspesifikasjonen fordi teorien sier at skjermingen virker gjennom $k_{\text{post}}$, ikke gjennom et nivåskift (@sec-met-kalender); den testes som kandidat i robusthetsanalysen, slik at et eventuelt residualt nivåløft blir et *funn* og ikke en antakelse. Og `strom_belop` inngår ikke i antallsmodellene: engangsutbetalinger endret beløp, ikke berettigelse.

**Transformasjon og tilbaketransformasjon.** Modelleringen skjer på logskala (@sec-met-kovariater). Punktprognosen i @eq-estimand er *medianen*, og medianen tilbaketransformeres ved ren eksponensiering. Det er et bevisst valg og ikke en forglemmelse: en forventningsriktig tilbaketransformasjon ville krevd et Jensen-ledd og gitt et annet estimand enn det forskningsspørsmålet definerer. Intervallgrensene tilbaketransformeres på samme måte, hvilket bevarer den asymmetrien nivåskalaen skal ha.

**Ordensvalg — og hva informasjonskriterier *ikke* brukes til.** $(p,d,q)(P,D,Q)_{12}$ velges automatisk ved AICc, med differensieringsgradene bestemt av enhetsrot- og sesongstyrketest (@sec-met-diagnostikk). Det avgjørende er *når*: valget gjentas ved hver prognoseopprinnelse, på treningsvinduet alene. Å velge orden én gang på hele utvalget og deretter «rulle» er en av de vanligste og minst synlige formene for informasjonslekkasje i denne litteraturen, og den ville gjort evalueringen optimistisk uten å etterlate spor.

AICc velger altså orden *innenfor* en modell. Den velger ikke mellom trinnene i @tbl-modeller, og det er et bevisst valg med to begrunnelser. Den ene er at stigen ikke er en søkeliste, men en dekomponering: å plukke AICc-vinneren ville gjort M6-mot-M4 og M6-mot-M5 meningsløse, og vi ville sittet igjen med én modell og ingen svar på hvor gevinsten kom fra. Den andre er teknisk. AICc er et *ettstegs* kriterium — det estimerer forventet ut-av-utvalgs-tilpasning for $h = 1$, betinget på at modellklassen er riktig og prosessen stabil. Forskningsspørsmålet gjelder $h = 1,\dots,12$ og intervalldekning, og et ettstegskriterium sier ingenting om noen av delene. Når selve målet lar seg måle direkte på rullerende opprinnelse, ville det vært et skritt tilbake å velge på en indikator for én av horisontene.

Det finnes en tredje mulighet som ikke forfølges her, men som fortjener å navngis: å la hver opprinnelse velge spesifikasjon etter *historisk ut-av-utvalgs*-ytelse, eller å kombinere modellene. Det er trolig nærmere det en analysefunksjon i drift ville gjort, og det er en utvidelse av designet — ikke en innvending mot stigen.

### M7: effektstørrelse hentet utenfra {#sec-met-prior}

M6 kan ikke framskrive en regelendring som ennå ikke har inntruffet: en regressor som er null gjennom hele treningsvinduet, har ingen koeffisient å estimere. Omfanget kvantifiseres i @sec-met-identifikasjon — det gjelder over halvparten av prognosepunktene. M7 lukker det hullet ved å hente effektstørrelsen fra en datert kilde utenfor serien. Modellen er identisk med M6 bortsett fra ett ledd:

$$\ln M^{T}_{t} \;=\; \beta'_{\text{est}} x^{\text{est}}_t \;+\; \underbrace{\textstyle\sum_{j \in \mathcal{U}_\tau} \lambda_j\, x^{j}_t}_{\text{pålagt, ikke estimert}} \;+\; \eta_t$$ {#eq-prior}

der $\mathcal{U}_\tau$ er regressorene som ikke passerer inngangsregelen ved opprinnelse $\tau$. Leddet er en *offset*: det inngår i prognosen, men ikke i estimeringen, slik at det ikke forstyrrer de øvrige koeffisientene.

**Hvor $\lambda_j$ kommer fra.** Fra `forhandsanslag.csv`, som kobler hver intervensjon til et datert anslag med kilde og verifiseringsstatus. Det nasjonale anslaget skaleres med Oslos målte andel av mottakerne, `r pst(osloandel)`, og omregnes til logskala mot nivået ved opprinnelsen. Anslagene er av tre slag, og forskjellen mellom dem er ikke kosmetisk: *forhåndsanslag* er publisert før hendelsen og er det eneste en prognose i drift faktisk ville hatt; *etterberegnet* er realisert utfall og kan bare brukes til å validere metoden, aldri i en prognose; *mekanisk* er utledet av regelverket selv.

**Begrunnelsen er ikke bekvemmelighet, men at det ikke finnes noe nøytralt alternativ.** Å utelate regressoren pålegger $\lambda_j = 0$ — en påstand om at en vedtatt regelendring ikke vil ha effekt. Valget står altså mellom å pålegge null og å pålegge det beste daterte tallet som finnes; ingen av delene er antakelsesfri. M7 gjør valget eksplisitt og kildebelagt i stedet for implisitt.

**Og prioren skal scores, ikke tros.** Tre hendelser har både et publisert anslag og en koeffisient som *er* estimerbar: koronavinduet, strømvinduets åpning og avviklingen i april 2024. Metoden kan derfor kalibreres — sammenlign $\hat\beta$ mot den prior-impliserte $\lambda$ der begge finnes. Treffer de publiserte tallene, er det belegg for å bruke dem der estimering er umulig; treffer de ikke, er *det* funnet, og det forteller hvor mye vekt en konsekvensutredning tåler. Sporingen rapporteres som egen figur i kapitlets andre del.

**Kalibreringen må være modellbasert, ikke en før-og-etter-differanse.** Det er fristende å måle et anslag ved å sammenlikne nivået før og etter hendelsen. Det holder ikke her, av tre grunner som alle er dokumentert tidligere i kapitlet: positive endringer fases inn gradvis (@sec-teori-beholdning), slik at en kort etterperiode undervurderer effekten; hendelsene overlapper, slik at nedgangen etter april 2024 delvis motvirkes av høstpakken fra juli; og kalendersagtannen på syv til ti prosent (@sec-met-observasjoner) dominerer enhver enkeltmåneds differanse. En rå sammenlikning gir derfor forhold mellom realisert og publisert som spriker fra om lag en halv til fire femdeler, uten at spriket sier noe om anslagene — det sier noe om at metoden er feil. T5 er formulert mot $\hat\beta$ nettopp fordi modellen kontrollerer for alle tre.

**En kjent begrensning, søkt etter og ikke funnet.** For høstpakken 2024 — regressoren som faktisk er uidentifisert ved flest opprinnelser — finnes ingen publiserte husstandstall. Fire kildefamilier er gjennomsøkt: Husbankens årsrapporter 2024 og 2025, revidert nasjonalbudsjett (Prop. 104 S), Husbankens egen pressemelding om revidert budsjett, og finanskomiteens innstilling. Alle oppgir kroner (85, 22 og 8 millioner), ingen oppgir husstander. For oppvarmingstillegget ville en omregning fra kroner dessuten vært direkte feil, siden tillegget gikk til *eksisterende* mottakere og altså hevet beløp framfor å utvide kretsen. Radene står som `ingen` i `forhandsanslag.csv` med søkedato og kildeliste. M7 degenererer da til M6 for den hendelsen — noe som er riktig oppførsel, men som betyr at metoden ikke kan demonstreres der.

**Asymmetriproposisjonen.** @eq-regarima estimerer statiske regresjonskoeffisienter og uttrykker dermed rene trinn, ikke transferfunksjonens $\delta$ (@eq-transfer). Asymmetriproposisjonen fra @sec-teori-beholdning testes derfor på to måter i stedet for å pålegges. For det første estimeres en variant der `win_strom` suppleres med en tre måneders innfasingsrampe ved inn-kanten; rampekoeffisienten er det direkte estimatet av gradvis innfasing, og variantene sammenliknes med AICc. For det andre inspiseres residualene ved hver kant: en skarp ut-kant skal ikke etterlate mønster, mens en gradvis inn-kant modellert som trinn skal etterlate en serie residualer med samme fortegn. De to testene peker på samme parameter fra hver sin side.

## Prognoseprotokoll {#sec-met-protokoll}

```{r}
#| label: protokoll
#| include: false

MIN_TRENING <- 72   # minste treningsvindu: 6 år, nok til sesong + 5 regressorer
H_MAKS      <- 12

opprinnelser <- oslo_est$dato[
  seq_len(nrow(oslo_est)) >= MIN_TRENING &
  seq_len(nrow(oslo_est)) <= (nrow(oslo_est) - H_MAKS)
]
n_opprinnelser <- length(opprinnelser)
n_modeller     <- 8
n_prognosepkt  <- n_opprinnelser * H_MAKS
```

Designet er rullerende opprinnelse med ekspanderende vindu [@tashman2000], som er den gyldige kryssvalideringsformen for avhengige data [@bergmeir2012]. Protokollen er fullstendig bestemt av tre parametre og utleder resten fra estimeringsutvalget:

*Treningsminimum.* `r nb(MIN_TRENING)` observasjoner — seks år. Det er nok til å identifisere en sesongkomponent og fem regressorer med margin, og korter man det ned, estimeres de første modellene på for lite data til at sammenlikningen sier noe om modellene framfor om utvalgsstørrelsen.

*Første opprinnelse* blir dermed `r mnd(min(opprinnelser))`, *siste* `r mnd(max(opprinnelser))` — den siste måneden der hele horisonten $h = 1,\dots,`r H_MAKS`$ ligger innenfor observerte data. Det gir `r nb(n_opprinnelser)` opprinnelser, `r nb(H_MAKS)` prognoser hver, altså `r nb(n_prognosepkt)` prognosepunkter per modell og `r nb(n_opprinnelser)` observasjoner per horisont. Med `r nb(n_modeller)` modeller blir det `r nb(n_modeller * n_opprinnelser)` modelltilpasninger og `r nb(n_modeller * n_prognosepkt)` prognosepunkter i alt.

*Evalueringsvinduet* strekker seg dermed fra `r mnd(min(opprinnelser) %m+% months(1))` til `r est_til`. Det er ikke en tilfeldig periode: den inneholder avviklingen i termin april 2024 — seriens største enkeltbevegelse (@sec-met-observasjoner) — høstpakken samme år og skjermingen fra januar 2025. Evalueringen stresstestes altså på nøyaktig de hendelsene studien handler om, og ikke bare på rolige måneder.

Fire regler gjelder ved hver opprinnelse.

1.  **Alt re-estimeres.** Ordensvalg, regresjonskoeffisienter og feilleddets parametre bestemmes på nytt fra treningsvinduet. Ingenting bæres over fra en tidligere opprinnelse.
2.  **Ingen informasjon utover opprinnelsen.** Treningsvinduet slutter ved opprinnelsen, og enhver modellbeslutning tas på det vinduet alene. De deterministiske regressorene er unntaket som beviser regelen: de er *per konstruksjon* kjent for alle framtidige måneder — regelverkskalenderen fordi vedtakene er fattet, meldekortgridet fordi det er en kalender (@sec-met-kalender). Nettopp derfor kan de brukes på hele horisonten uten å bryte disiplinen, og nettopp derfor er de verdifulle.
3.  **Vedtatt, men ikke ikrafttrådt, regelverk regnes som kjent.** Det er den operative situasjonen en prognose i drift står i, og det er innholdet i underspørsmål **UB**.
4.  **Pseudo-sanntid, ikke sanntid.** Serien revideres bakover ved etterkontroll mot skatteoppgjøret, og historiske vintages er ikke offentlig tilgjengelige (@sec-met-dv). Evalueringen bruker derfor gjeldende vintage gjennomgående. Det er en kjent optimistisk skjevhet, den gjelder alle modellene likt, og den er den eneste avviket fra ekte sanntidsdisiplin i protokollen.

**Stratifisering.** Hvert prognosepunkt merkes som *bruddnært* dersom målmåneden ligger innenfor tre måneder av en datert intervensjon fra `intervensjonstabell.csv`, ellers som *rolig*. Evalueringen rapporteres samlet og stratifisert. Grunnen er at gjennomsnittet over alle måneder kan skjule det som betyr mest: en modell som er marginalt bedre i rolige perioder, men vesentlig bedre rundt bruddene, er den nyttige modellen i drift — og den forskjellen forsvinner i et samlet MASE-tall.

### En regressor kan ikke estimeres før hendelsen har skjedd {#sec-met-identifikasjon}

Protokollen over har en konsekvens som er lett å overse fram til den bryter kjøringen, og som endrer hva studien kan konkludere med. En intervensjonsregressor er null helt til hendelsen inntreffer. Ved en prognoseopprinnelse *før* hendelsen er kolonnen derfor identisk null i treningsvinduet, designmatrisen er rangdefekt, og koeffisienten finnes ikke — uansett hvor godt regelendringen er dokumentert i regelverkskalenderen.

@tbl-identifikasjon viser omfanget. Det er ikke marginalt.

```{r}
#| label: tbl-identifikasjon
#| tbl-cap: "Identifiserbarhet per prognoseopprinnelse. «Ikke-null i trening» er antall observasjoner der regressoren er forskjellig fra null i treningsvinduet; en regressor med færre enn seks slike kan ikke estimeres meningsfullt. «Rammede punkter» teller prognosepunkter der regressoren er aktiv i målmåneden, men ikke var estimerbar ved opprinnelsen."

MIN_NZ <- 6   # minste antall ikke-null observasjoner for at en regressor slipper inn
REG    <- c("win_covid", "win_strom", "pakke_2024h2", "k_pre", "k_post")

X_est <- oslo_est |> select(dato) |> left_join(X, by = "dato")

# Er regressoren estimerbar i treningsvinduet ved hver opprinnelse?
estimerbar <- expand_grid(opprinnelse = opprinnelser, regressor = REG) |>
  mutate(nz = map2_int(opprinnelse, regressor,
                       \(t0, r) sum(X_est[[r]][X_est$dato <= t0] != 0)),
         ok = nz >= MIN_NZ)

# Er regressoren aktiv i målmåneden?
aktiv <- expand_grid(opprinnelse = opprinnelser, h = 1:H_MAKS, regressor = REG) |>
  mutate(maal = opprinnelse %m+% months(h),
         paa  = map2_lgl(maal, regressor, \(m, r) {
           v <- X[[r]][X$dato == m]; length(v) == 1 && v != 0
         }))

# Et prognosepunkt er rammet dersom en aktiv regressor ikke var estimerbar
rammet <- aktiv |>
  left_join(estimerbar, by = c("opprinnelse", "regressor")) |>
  mutate(mangler = paa & !ok)

celler <- rammet |> summarise(udekket = sum(mangler), .by = c(opprinnelse, h))
andel_rammet <- 100 * mean(celler$udekket > 0)

estimerbar |>
  summarise(uten = sum(!ok),
            inn  = if (any(ok)) mnd(min(opprinnelse[ok])) else "aldri",
            .by = regressor) |>
  left_join(rammet |> summarise(punkter = sum(mangler), .by = regressor),
            by = "regressor") |>
  transmute(Regressor = regressor,
            `Opprinnelser uten identifikasjon` = uten,
            `Første opprinnelse der den slipper inn` = inn,
            `Rammede punkter` = punkter) |>
  kab(align = "lrlr")
```

Av de `r nb(n_prognosepkt)` prognosepunktene er `r pst(andel_rammet, 0)` slik at minst én aktiv regressor ikke kunne estimeres ved opprinnelsen. Andelen stiger med horisonten — fra `r pst(100 * mean(celler$udekket[celler$h == 1] > 0), 0)` på én måned til `r pst(100 * mean(celler$udekket[celler$h == H_MAKS] > 0), 0)` på tolv — fordi lengre horisonter når lenger inn i regelendringer som ennå ikke har inntruffet.

**Men ikke alle uidentifiserte regressorer er like skadelige.** Å utelate en regressor er ikke et nøytralt valg — det pålegger $\beta = 0$. Om det er galt, avhenger av hva teorien sier at koeffisienten skal være.

For `k_post` er null *nøyaktig* det teorien predikerer: når to tredeler av ytelsen medregnes i tre-utbetalingsmåneder, blir telt inntekt $3 \times \tfrac{2}{3} = 2$ — identisk med en normalmåned (@sec-met-kalender). Utelatelse og korrekt prior sammenfaller dermed, og de `r nb(sum(rammet$mangler[rammet$regressor == "k_post"]))` rammede punktene for `k_post` er rammet på en ufarlig måte. For `pakke_2024h2` er situasjonen motsatt: både regelverket og kronebeløpene i årsrapporten sier at effekten er positiv, og å utelate regressoren pålegger en påstand kildene motsier.

Det er også verdt å merke seg hva som *ikke* er et problem. Seriens største brudd — avviklingen i termin april 2024 — bæres av `win_strom`, som er identifisert ved alle opprinnelser fra `r mnd(min(opprinnelser))`, fordi vinduets åpning ligger i treningsvinduet. Modellen kan derfor framskrive lukkingen selv om lukkingen selv ligger i horisonten. Identifikasjonsproblemet rammer altså ikke den hendelsen studien er mest opptatt av.

**Protokollens svar: en eksplisitt inngangsregel.** En regressor inngår i spesifikasjonen ved opprinnelse $\tau$ hvis og bare hvis den har minst `r nb(MIN_NZ)` ikke-null observasjoner i treningsvinduet; ellers utelates den for den opprinnelsen. Regelen er deterministisk og logges, slik at spesifikasjonen ved hver opprinnelse er kjent. Alternativet — å la modelltilpasningen selv droppe kollineære kolonner — ville gitt samme resultat i stillhet, og «M6» ville da vært syv ulike modeller uten at noen sto oppført.

**Og en presisering av hva studien kan konkludere med.** Gevinsten av å modellere kjente regelendringer eksplisitt deler seg i to regimer som må rapporteres hver for seg:

*Regelendringen har trådt i kraft.* Koeffisienten er estimert på observerte data, og modellen kan framskrive nivået. Dette er det gunstige regimet, og det er her T2 skal slå ut.

*Regelendringen er vedtatt, men ikke trådt i kraft.* Koeffisienten finnes ikke i serien. Regelverkskalenderen forteller modellen at noe kommer og nøyaktig når — men ikke hvor mye. En prognose kan da bare bære endringen dersom effektstørrelsen hentes *utenfor* serien.

Det andre regimet er ikke en svakhet ved designet; det er innholdet i underspørsmål **UB**. Studien har kilden som trengs: for avviklingen i april 2024 forelå departementets anslag på 20 000–25 000 mottakere nasjonalt *på forhånd* [@dagsavisen2024; @pensjonistforbundet2024], og Husbankens årsrapport bekreftet om lag 25 000 i etterkant [@husbanken2024aars]. Det er grunnlaget for M7 (@sec-met-prior), som pålegger effektstørrelsen fra en datert kilde der den ikke kan estimeres — og som selv blir målt, ved at prioren sammenliknes med den estimerte koeffisienten på de hendelsene der begge finnes.

## Evaluering og inferens {#sec-met-evaluering}

**Punktpresisjon.** MAE og RMSE per horisont, og MASE (@eq-mase) som skalafritt hovedmål. Nevneren er sesongnaiv gjennomsnittlig absoluttfeil beregnet *innenfor treningsvinduet ved hver opprinnelse* [@hyndmankoehler2006] — ikke på hele utvalget, som ville gjort nevneren avhengig av data modellen ikke hadde sett.

**Signifikans, og en fallgruve som må navngis.** Forskjeller i prognosepresisjon testes med Diebold–Mariano [@diebold1995] med småutvalgskorreksjon [@harvey1997]. Testen forutsetter imidlertid ikke-nøstede modeller, og de sammenlikningene vi er mest interessert i — M6 mot M3, M6 mot M4, M6 mot M5 — er nøstede i populasjonen: den enklere modellen er den generelle med koeffisienter satt til null. Under nullhypotesen blir tapsdifferansen degenerert, og DM-testen er da underdimensjonert. Protokollen håndterer dette eksplisitt:

*   nøstede par testes med Clark–Wests justerte statistikk, som korrigerer for nettopp dette;
*   ikke-nøstede par (for eksempel M6 mot M2) testes med DM–Harvey;
*   og hovedbeviset for forskningsspørsmålet hviler uansett ikke på en punktprognosetest alene, men på intervalldekningen, der problemet ikke oppstår på samme form.

Å velge test etter om paret er nøstet, er ikke pedanteri: en DM-test som feilaktig ikke forkaster, ville fått studien til å konkludere med at regelverksinformasjon *ikke* hjelper, når den i virkeligheten bare hadde brukt feil test.

**Stratifisering på identifikasjon.** I tillegg til bruddnær og rolig stratifiseres evalueringen på om alle aktive regressorer var estimerbare ved opprinnelsen (@sec-met-identifikasjon). Uten den delingen blander hovedtallet sammen to regimer som svarer på hvert sitt spørsmål, og gjennomsnittet undervurderer M6 der modellen faktisk har informasjonen — samtidig som det overvurderer den der modellen umulig kan ha den.

**Intervaller.** Empirisk dekning på 80 og 95 prosent, rapportert per horisont, sammen med intervallskåren i @eq-winkler — en strengt proper skåringsregel som ikke kan spilles ved å være systematisk for bred eller for smal [@gneiting2007]. Dekning uten skarphet er verdiløs: et intervall fra null til uendelig dekker alltid. De to rapporteres derfor sammen, aldri hver for seg.

**Konformal etterkalibrering.** Der modellintervallene underdekker — forventet rundt brudd [@makridakis2022b] — kalibreres de konformalt. Skjemaet er rullerende og respekterer tidsretningen: for horisont $h$ og opprinnelse $\tau$ bygges intervallet av den empiriske kvantilen til $|e_h|$ fra *alle opprinnelser strengt før* $\tau$. Kalibreringssettet inneholder dermed aldri informasjon fra prognosepunktet det kalibrerer. Dekningsgarantien i den klassiske konformale teorien hviler på utvekslbarhet [@vovk2005; @romano2019], som en tidsserie med brudd ikke oppfyller; garantien er derfor tilnærmet, og varianten er den som er utviklet for avhengige data [@xu2023]. Denne forutsetningen sies her framfor å underforstås, siden en eksakt garanti er nettopp det metoden ofte selges på.

**Residualdiagnostikk.** Ljung–Box på standardiserte residualer med frihetsgrader justert for estimerte parametre, ACF og PACF, og fordelingssjekk av de standardiserte residualene. Én presisering: normalitet i residualene er ikke et mål i seg selv. Det som avgjør intervallenes kvalitet, er den *prediktive* fordelingen ut av utvalget, og den måles av dekning og intervallskår. Residualdiagnostikken brukes til å finne umodellert struktur — særlig ved intervensjonskantene (@sec-met-modeller) — ikke til å attestere modellen.

**Fra teori til teststatistikk.** De fire implikasjonene i @sec-teori-syntese oversettes i @tbl-tester til fire avgjørbare størrelser:

| | Implikasjon | Operasjonalisering | Faller dersom |
|:----|:-----------------|:-----------------------|:----------------------|
| **T1** | Struktur slår naive referanser | MASE for M6 mot M1 og M0, per horisont 1–12 | MASE $\geq 1$ på flertallet av horisontene |
| **T2** | Eksplisitte intervensjoner hjelper — mest på intervallene | M6 mot M3, stratifisert på bruddnære og rolige punkter *og* på om regressoren var estimerbar (@sec-met-identifikasjon); Clark–West på punktfeil, dekningsdifferanse på intervaller | ingen dekningsgevinst i de bruddnære vinduene der regressoren var estimerbar |
| **T3** | Kalendereffekten er stor før 2025 og eksakt null etter | $\hat\beta_{k,\text{pre}}$ negativ og signifikant; for $\hat\beta_{k,\text{post}}$ en ekvivalenstest mot grensen $\delta = \tfrac{1}{2}\lvert\hat\beta_{k,\text{pre}}\rvert$ | $\hat\beta_{k,\text{pre}}$ ikke negativ, eller ekvivalens for $\hat\beta_{k,\text{post}}$ forkastes |
| **T4** | Nominell dekning krever brudd og kalender; resten fikses konformalt | dekning ved 80/95 for M3 og M6, før og etter konformal kalibrering, med intervallskår | M6 dekker ikke bedre enn M3, eller kalibreringen ikke lukker gapet |
| **T5** | Daterte forhåndsanslag bærer informasjon der estimering er umulig | M7 mot M6 på punkter der en regressor var uidentifisert (@sec-met-prior); og $\hat\beta$ mot prior-implisert $\lambda$ der begge finnes | M7 er ikke bedre enn M6 på de rammede punktene, eller $\hat\beta$ avviker systematisk fra anslaget |

: Teoriens implikasjoner som avgjørbare teststatistikker. Kolonnen «faller dersom» er skrevet før estimeringen, slik at utfallet ikke kan omtolkes i etterkant. {#tbl-tester}

T5 er ny og hører til underspørsmål **UB**. Den er formulert med to ledd fordi de svarer på hvert sitt: det første måler om metoden hjelper, det andre om kilden er til å stole på. En metode som hjelper med en prior som ikke stemmer, er et lykketreff og ikke et funn.

To ting i tabellen fortjener en kommentar. **T3** er formulert som en *ekvivalenstest*, ikke som en vanlig nullhypotesetest. Grunnen er at teorien predikerer at effekten er null, og en ordinær test kan aldri bekrefte en nullhypotese — den kan bare unnlate å forkaste, hvilket like gjerne kan skyldes lite data. Ekvivalensformuleringen snur bevisbyrden riktig vei: den krever at konfidensintervallet for $\hat\beta_{k,\text{post}}$ ligger *innenfor* en grense vi har satt på forhånd, og gir dermed teorien en reell mulighet til å falle. Grensen er satt til halvparten av den estimerte førperiodeeffekten, altså i seriens egne enheter framfor en vilkårlig konstant. **T2** er formulert med intervalldekning som det avgjørende kriteriet, ikke punktfeil. Det er ikke en flukt fra det vanskelige målet, men følger av @clements1998: umodellerte nivåskift skader intervallene mer og lenger enn de skader punktprognosene, og det er der gevinsten skal vise seg om teorien stemmer.

## Reproduserbarhet {#sec-met-repro}

Kontrakten fra kapitlets åpning gjelder også modelldelen: hver størrelse skal kunne regenereres av navngitt kode fra navngitt kilde.

*Beregningen ligger i dokumentet.* Estimering, rullerende kryssvalidering og diagnostikk kjører når `unt_1.qmd` bygges. Det gir den sterkeste formen for konsistens mellom tekst og tall — de kan ikke komme i utakt, fordi de er samme kjøring — og prisen er byggetid: `r nb(n_modeller * n_opprinnelser)` modelltilpasninger per render. Blir det upraktisk, er `execute: freeze: auto` i YAML-hodet den dokumenterte utveien; den fryser chunk-resultatene til kildefilen endres, uten å endre noe ved beregningen.

*Determinisme.* Løypa inneholder ingen tilfeldighet: ordensvalget er en deterministisk søkeprosedyre, den konformale kalibreringen er empiriske kvantiler, og kalenderfasen kalibreres fra data (@sec-met-kalender). To renderinger av samme kildefil mot samme datapakke gir derfor identisk resultat, og det er kontrollert ved å sammenlikne tekstinnholdet i to bygg framfor å anta det. Seeds settes likevel eksplisitt, slik at stokastiske metoder kan legges til senere uten at determinismen stilltiende forsvinner.

*Miljø.* Pakkeversjonene låses med `renv`, og låsefilen versjoneres. R- og Quarto-versjon, samt datapakkens uttrekksdato (`r UTTREKKSDATO`), stemples i dokumentet. Typst-malen bruker bare fonter Typst har innebygd; det er et typografisk valg, men det hører hjemme her, fordi det er grunnen til at PDF-en blir identisk uansett vertsmaskin.

*Kart over løypa.* Datalasting og verifikasjon i chunkene `oppsett` til `tbl-kontroller`; regressorkonstruksjon i `met-intervensjoner` og `met-kalender-reg`; analysesettet i `met-analysesett`; protokollparametrene i `protokoll`. Uttrekksskriptene som produserer datapakken, ligger i `velferdsetaten-data/scripts/` og er dokumentert i `datakilder.md` og `kodebok.md`.

*Feil skal stoppe byggingen, ikke passere stille.* Verifikasjonskontrollene i @sec-met-verifikasjon avbryter renderingen ved avvik. Samme prinsipp gjelder modelldelen: dimensjonskrav på analysesettet, krav om at de deterministiske regressorene er komplette over hele prognosehorisonten, og krav om at antall prognosepunkter per horisont stemmer med protokollen, håndheves med eksplisitte krav i koden framfor å kontrolleres ved øyemål.

# Resultater {#sec-res}

Kapitlet rapporterer den rullerende kryssvalideringen som @sec-met-protokoll spesifiserer, og holder utfallet opp mot «faller dersom»-kolonnen i @tbl-tester. Tre av de fem implikasjonene holder, én holder i retning men ikke i styrke, og én faller — motsatt vei av det som var forventet.

```{r}
#| label: res-cv
#| include: false
#| cache: true
#| cache.extra: !expr list(nrow(analyse), max(analyse$dato), UTTREKKSDATO)

# --- Rullerende opprinnelse. Dette er dokumentets tunge beregning: åtte modeller
# --- ved hver av opprinnelsene, med ordensvalg og re-estimering per opprinnelse.
LAMBDA_ORAKEL <- NULL   # settes under; full-utvalgsestimatet for pakke_2024h2

kjor_opprinnelse <- function(t0) {
  tr <- analyse |> mutate(mnd = yearmonth(dato)) |> filter(dato <= t0) |> as_tsibble(index = mnd)
  nd <- analyse |> mutate(mnd = yearmonth(dato)) |>
    filter(dato > t0, dato <= t0 %m+% months(H_MAKS)) |> as_tsibble(index = mnd)

  # Inngangsregelen fra @sec-met-identifikasjon
  slipper <- function(v) v[map_lgl(v, \(r) sum(tr[[r]] != 0) >= MIN_NZ)]
  iv  <- slipper(c("win_covid", "win_strom", "pakke_2024h2"))
  kal <- slipper(c("k_pre", "k_post"))
  ute <- setdiff(REG, c(iv, kal))
  form <- function(v) if (length(v)) as.formula(paste("log_M ~", paste(v, collapse = " + "))) else log_M ~ 1

  fit <- tr |> model(
    M0 = NAIVE(log_M), M1 = SNAIVE(log_M ~ lag("year")), M2 = ETS(log_M), M3 = ARIMA(log_M),
    M4 = ARIMA(form(kal)), M5 = ARIMA(form(iv)), M6 = ARIMA(form(c(iv, kal))))

  ut <- forecast(fit, new_data = nd, times = 0) |> as_tibble() |>
    transmute(.model, dato = as.Date(mnd), mu = .mean,
              sd = sqrt(distributional::variance(log_M))) |>
    left_join(nd |> as_tibble() |> select(dato, log_M_sant = log_M, M_sant = M), by = "dato") |>
    mutate(opprinnelse = t0, h = as.integer(round(as.numeric(dato - t0) / 30.44)),
           ute = paste(ute, collapse = ","))

  # M7: pålagt bidrag der regressoren ikke slapp inn. Anslaget er her
  # full-utvalgskoeffisienten, ikke et publisert tall (se @sec-res-prior).
  m7 <- ut |> filter(.model == "M6") |>
    left_join(nd |> as_tibble() |> select(dato, pakke_2024h2), by = "dato") |>
    mutate(.model = "M7",
           mu = mu + if ("pakke_2024h2" %in% ute) LAMBDA_ORAKEL * pakke_2024h2 else 0) |>
    select(-pakke_2024h2)
  bind_rows(ut, m7)
}

LAMBDA_ORAKEL <- analyse |> mutate(mnd = yearmonth(dato)) |> as_tsibble(index = mnd) |>
  model(m = ARIMA(log_M ~ win_covid + win_strom + pakke_2024h2 + k_pre + k_post)) |>
  tidy() |> filter(term == "pakke_2024h2") |> pull(estimate)

cv <- map_dfr(opprinnelser, kjor_opprinnelse)
stopifnot(nrow(cv) == length(opprinnelser) * H_MAKS * 8,
          !anyNA(cv$mu), !anyNA(cv$sd), all(cv$sd > 0),
          all(cv$dato == cv$opprinnelse %m+% months(cv$h)))

# --- Evaluering
z80 <- qnorm(0.90); z95 <- qnorm(0.975)
nevner <- map_dfr(opprinnelser, \(t0) {
  x <- oslo_est |> filter(dato <= t0) |> arrange(dato)
  tibble(opprinnelse = t0, q = mean(abs(x$M - lag(x$M, 12)), na.rm = TRUE))
})
iv_dato <- bind_rows(d$intervensjoner |> filter(!is.na(termin_fra)) |> transmute(dato = termin_fra),
                     d$intervensjoner |> filter(!is.na(termin_til)) |> transmute(dato = termin_til))

R <- cv |> left_join(nevner, by = "opprinnelse") |>
  mutate(pred = exp(mu), e = M_sant - pred, mase = abs(e) / q, le = log_M_sant - mu,
         l80 = exp(mu - z80*sd), u80 = exp(mu + z80*sd),
         l95 = exp(mu - z95*sd), u95 = exp(mu + z95*sd),
         dekk80 = M_sant >= l80 & M_sant <= u80, dekk95 = M_sant >= l95 & M_sant <= u95,
         ws80 = (u80-l80) + 10*(l80-M_sant)*(M_sant<l80) + 10*(M_sant-u80)*(M_sant>u80),
         pakke_ute = str_detect(ute, "pakke_2024h2"),
         bruddnaer = map_lgl(dato, \(m) any(abs(as.numeric(m - iv_dato$dato))/30.44 <= 3)))

# Konformal kalibrering: kvantil av |logfeil| fra alle TIDLIGERE opprinnelser
K <- R |> arrange(.model, h, opprinnelse) |> group_by(.model, h) |>
  mutate(q80 = map_dbl(row_number(), \(i) if (i <= 3) NA_real_ else quantile(abs(le[seq_len(i-1)]), .80, names = FALSE)),
         q95 = map_dbl(row_number(), \(i) if (i <= 3) NA_real_ else quantile(abs(le[seq_len(i-1)]), .95, names = FALSE))) |>
  ungroup() |>
  mutate(kdekk80 = M_sant >= exp(mu-q80) & M_sant <= exp(mu+q80),
         kdekk95 = M_sant >= exp(mu-q95) & M_sant <= exp(mu+q95),
         kws80 = (exp(mu+q80)-exp(mu-q80)) + 10*(exp(mu-q80)-M_sant)*(M_sant<exp(mu-q80)) +
                 10*(M_sant-exp(mu+q80))*(M_sant>exp(mu+q80))) |>
  filter(!is.na(q80))

# Diebold–Mariano / Clark–West
dmtest <- function(a, b, nested, hh) {
  x <- R |> filter(.model %in% c(a,b), h == hh) |> select(.model, opprinnelse, e) |>
    pivot_wider(names_from = .model, values_from = e) |> drop_na()
  e1 <- x[[a]]; e2 <- x[[b]]; n <- length(e1)
  dd <- if (nested) e1^2 - (e2^2 - (e1-e2)^2) else e1^2 - e2^2
  L <- hh - 1
  g <- as.numeric(acf(dd, lag.max = max(L,1), type = "covariance", plot = FALSE)$acf)
  v <- (g[1] + if (L > 0) 2*sum(g[2:(L+1)]) else 0) / n
  if (is.na(v) || v <= 0) return(NA_real_)
  (mean(dd)/sqrt(v)) * sqrt((n + 1 - 2*hh + hh*(hh-1)/n)/n)
}
MNAVN <- c(M0="M0 naiv", M1="M1 sesongnaiv", M2="M2 ETS", M3="M3 SARIMA",
           M4="M4 RegARIMA + k", M5="M5 RegARIMA + X", M6="M6 RegARIMA + X + k",
           M7="M7 med pålagt bidrag")
```

## Presisjon over horisonten {#sec-res-presisjon}

```{r}
#| label: tbl-res-samlet
#| tbl-cap: "Samlet resultat over alle prognosepunkter. MASE under 1 betyr bedre enn sesongnaiv prognose. Dekning er andelen realisasjoner innenfor intervallet; nominelt nivå er 80 og 95 prosent. Intervallskåren er @eq-winkler — lavere er bedre, og den straffer både for bred og for smal."

R |> summarise(MASE = mean(mase), RMSE = sqrt(mean(e^2)),
               d80 = 100*mean(dekk80), d95 = 100*mean(dekk95), ws = mean(ws80), .by = .model) |>
  arrange(MASE) |>
  transmute(Modell = MNAVN[.model], MASE = nb(MASE, 3), RMSE = nb(RMSE),
            `Dekning 80` = pst(d80, 0), `Dekning 95` = pst(d95, 0),
            `Intervallskår` = nb(ws)) |>
  kab(align = "lrrrrr")
```

@tbl-res-samlet samler utfallet. **T1 holder mot de naive referansene, men ikke mot alle.** M6 oppnår MASE `r nb(R |> filter(.model=="M6") |> pull(mase) |> mean(), 3)` mot sesongnaivs `r nb(R |> filter(.model=="M1") |> pull(mase) |> mean(), 3)` — struktur slår gjentakelse, som forventet. Men ETS ligger på `r nb(R |> filter(.model=="M2") |> pull(mase) |> mean(), 3)` og slår M6 samlet, og på tolv måneders horisont er forskjellen signifikant i ETS' favør (Diebold–Mariano `r nb(dmtest("M2","M6",FALSE,12), 2)`). Det er M4-konkurransens lærdom i praksis [@makridakis2020]: en enkel, veletablert metode er vanskelig å slå, og en modell som vet mer om regelverket vinner ikke automatisk på alle horisonter. @fig-res-mase viser hvordan forspranget forsvinner med horisonten.

```{r}
#| label: fig-res-mase
#| fig-cap: "MASE per horisont. Stiplet linje ved 1 er sesongnaiv prognose i treningsvinduet. Ingen modell dominerer over hele horisonten: de regelinformerte er klart best på korte horisonter, mens forspranget spises opp mot tolv måneder."
#| fig-width: 6.6
#| fig-height: 3.2

vis <- c("M1","M2","M3","M6","M7")
fg  <- c(M1="grey68", M2="grey45", M3="grey25", M6="#00505c", M7="#b3121d")
R |> filter(.model %in% vis) |> summarise(mase = mean(mase), .by = c(.model, h)) |>
  ggplot(aes(h, mase, colour = .model)) +
  geom_hline(yintercept = 1, linetype = "22", colour = "grey60", linewidth = .35) +
  geom_line(aes(linewidth = .model == "M6")) + geom_point(size = 1) +
  geom_text(data = ~filter(.x, h == 12), aes(label = MNAVN[.model]), hjust = -0.07, size = 2.4) +
  scale_colour_manual(values = fg) + scale_linewidth_manual(values = c(`FALSE`=.5, `TRUE`=.9)) +
  scale_x_continuous(breaks = c(1,3,6,9,12), expand = expansion(c(.02,.46))) +
  labs(x = "Horisont (måneder)", y = "MASE") +
  theme_minimal(base_size = 9) +
  theme(legend.position = "none", panel.grid.minor = element_blank(),
        panel.grid.major = element_line(linewidth = .25, colour = "grey88"),
        axis.title = element_text(size = 8, colour = "grey35"))
```

## Gevinsten av regelverksinformasjon {#sec-res-gevinst}

```{r}
#| label: tbl-res-cw
#| tbl-cap: "Sammenlikninger langs modellstigen. Clark–West brukes på nøstede par, Diebold–Mariano med Harvey-korreksjon på ikke-nøstede (@sec-met-evaluering). Positiv verdi favoriserer den første modellen; ensidig kritisk verdi ved 5 prosent er 1,64."

tribble(~par, ~a, ~b, ~nested,
        "M6 mot M3 — samlet verdi av deterministisk informasjon", "M3","M6", TRUE,
        "M6 mot M4 — regelverkets bidrag",                        "M4","M6", TRUE,
        "M6 mot M5 — kalenderens bidrag",                         "M5","M6", TRUE,
        "M6 mot M2 — mot en veletablert referanse",               "M2","M6", FALSE) |>
  mutate(map_dfr(seq_along(par), \(i) set_names(
    map(c(1,3,6,12), \(hh) nb(dmtest(a[i], b[i], nested[i], hh), 2)),
    c("h = 1","h = 3","h = 6","h = 12")))) |>
  transmute(Sammenlikning = par, Test = if_else(nested, "Clark–West", "DM–Harvey"),
            `h = 1`, `h = 3`, `h = 6`, `h = 12`) |>
  kab(align = "llrrrr")
```

@tbl-res-cw gjør sammenlikningene langs stigen eksplisitte. **T2 holder i retning, men ikke i styrke.** M6 slår M3 med `r pst(abs(100*(mean(R$mase[R$.model=="M6"])/mean(R$mase[R$.model=="M3"]) - 1)))` på MASE, og på hver enkelt horisont. Clark–West-statistikken er positiv gjennomgående, men når ikke kritisk verdi på noen horisont. Med `r nb(length(opprinnelser))` opprinnelser per horisont er testen rett og slett for svak til å avgjøre en forskjell av denne størrelsen; det er en utvalgsbegrensning, ikke et bevis for fravær av effekt.

**Kalenderregressoren hjelper på korte horisonter og skader på lange.** M6 mot M5 gir `r nb(dmtest("M5","M6",TRUE,1), 2)` ved én måned og `r nb(dmtest("M5","M6",TRUE,12), 2)` ved tolv. Sammenholdt med at M5 har lavere samlet MASE enn M6, er dette kapitlets tydeligste påminnelse om at innenfor-utvalgs-signifikans ikke er ut-av-utvalgs-verdi: kalendereffekten er utvilsomt reell (@sec-res-kalender), men den er en høyfrekvent svingning som betyr lite for nivået tolv måneder fram, og to ekstra parametre koster mer enn de smaker på et treningsvindu av denne lengden.

## Aprilbruddet 2024: hva regelverksinformasjon faktisk er verdt {#sec-res-april}

Gjennomsnittstall over 372 prognosepunkter skjuler den hendelsen studien er bygget for. @tbl-res-april viser alle prognoser mot termin april 2024 — seriens største enkeltbevegelse, et fall på `r pst(abs(apr24_pst))` på én termin.

```{r}
#| label: tbl-res-april
#| tbl-cap: "Alle prognoser mot termin april 2024, fra hver opprinnelse som når fram. Fasit er 15 588 husstander; nivået måneden før var 20 849. En modell som ikke kjenner regelendringen, må lande nær 20 000."

R |> filter(dato == as.Date("2024-04-01"), .model %in% c("M1","M3","M5","M6")) |>
  transmute(.model, h, pred = round(pred)) |>
  pivot_wider(names_from = .model, values_from = pred) |>
  arrange(h) |>
  transmute(`Horisont (mnd)` = h,
            `M1 sesongnaiv` = nb(M1), `M3 SARIMA` = nb(M3),
            `M5 RegARIMA + X` = nb(M5), `M6 RegARIMA + X + k` = nb(M6),
            `Fasit` = nb(15588)) |>
  kab(align = "rrrrrr")
```

Referansemodellene lander mellom 19 500 og 20 900 fra samtlige horisonter: de ser ikke bruddet, og kan ikke se det. M6 lander på `r nb(R |> filter(dato==as.Date("2024-04-01"), .model=="M6", h==9) |> pull(pred))` husstander fra ni måneders horisont, mot en fasit på 15 588 — et avvik på `r pst(abs(100*(R |> filter(dato==as.Date("2024-04-01"), .model=="M6", h==9) |> pull(pred))/15588 - 100))`. Å treffe et strukturelt fall på en fjerdedel innenfor tre prosent, ni måneder i forveien, er ikke en modelleringsprestasjon: det er informasjonen i regelverkskalenderen som gjør arbeidet, og det er hele poenget.

Mønsteret er dessuten motsatt av det vanlige — M6 er *bedre* på lang enn på kort horisont for denne hendelsen. Forklaringen ligger i modellen: autoregressivkoeffisienten er nær 0,9, så på korte horisonter trekkes prognosen mot marsnivået, mens intervensjonsregressoren dominerer først når AR-leddet har fått tid til å dø ut.

## Kalendereffekten før og etter skjermingen {#sec-res-kalender}

```{r}
#| label: tbl-res-t3
#| tbl-cap: "Kalenderregressorens koeffisienter i hovedspesifikasjonen, estimert på hele utvalget. Ekvivalensgrensen er satt på forhånd til halvparten av førperiodens estimerte effekt (@tbl-tester)."

fullfit <- analyse |> mutate(mnd = yearmonth(dato)) |> as_tsibble(index = mnd) |>
  model(M6 = ARIMA(log_M ~ win_covid + win_strom + pakke_2024h2 + k_pre + k_post))
ko <- tidy(fullfit)
kp <- ko |> filter(term == "k_pre"); kq <- ko |> filter(term == "k_post")
ki <- kq$estimate + c(-1,1)*qnorm(.975)*kq$std.error
delta <- abs(kp$estimate)/2

ko |> filter(term %in% c("win_covid","win_strom","pakke_2024h2","k_pre","k_post")) |>
  transmute(Regressor = term, Koeffisient = nb(estimate, 4), `Std.feil` = nb(std.error, 4),
            `t` = nb(statistic, 2), `Nivåeffekt` = pst(100*(exp(estimate)-1)),
            Forventet = c("opp","opp","opp","ned","null")) |>
  kab(align = "lrrrrl")
```

@tbl-res-t3 gir koeffisientene. **T3 holder i første ledd og faller i andre — og det er ekvivalensformuleringen som gjør forskjellen synlig.** Kalendereffekten før skjermingen er `r pst(100*(exp(kp$estimate)-1))` per ekstra meldekortutbetaling, med $t = `r nb(kp$statistic,2)`$: sterkt, riktig fortegn, og i samme størrelsesorden som det uavhengige anslaget fra fasekalibreringen (@tbl-kalender). Etter skjermingen er punktestimatet `r pst(100*(exp(kq$estimate)-1))` — så nær null som teorien kunne håpe på.

Men 95 %-konfidensintervallet er $[`r nb(ki[1],4)`,\ `r nb(ki[2],4)`]$, og ekvivalensgrensen $\delta = `r nb(delta,4)`$. Intervallet er bredere enn grensen, og ekvivalens er dermed **ikke** etablert. Presist sagt: dataene utelukker at kalendereffekten overlevde i full styrke — konfidensintervallets ytterpunkt `r nb(max(abs(ki)),4)` ligger under førperiodens `r nb(abs(kp$estimate),4)` — men de kan ikke utelukke at den overlevde i halv styrke. Årsaken er utvalget: bare `r nb(sum(analyse$dato >= as.Date("2025-01-01")))` måneder ligger etter regimeskiftet, og bare et fåtall av dem er tre-utbetalingsmåneder.

En ordinær nullhypotesetest ville her gitt $p = `r nb(kq$p.value, 3)`$ og invitert til konklusjonen «ingen effekt, T3 bekreftet». Det ville vært å forveksle fravær av bevis med bevis for fravær. Ekvivalenstesten sier det riktige: punktestimatet støtter teorien, men datagrunnlaget er ennå ikke sterkt nok til å *etablere* den.

## Intervalldekning: implikasjonen som falt {#sec-res-dekning}

```{r}
#| label: fig-res-dekning
#| fig-cap: "Empirisk dekning ved nominelt 80 prosent, før og etter konformal kalibrering. Åpent punkt er modellens eget intervall, fylt punkt etter kalibrering. De regelinformerte modellene har de smaleste intervallene og den dårligste dekningen — og kalibreringen lukker mesteparten av gapet."
#| fig-width: 6.4
#| fig-height: 2.6

K |> filter(.model %in% vis) |>
  summarise(For = 100*mean(dekk80), Etter = 100*mean(kdekk80), .by = .model) |>
  pivot_longer(c(For, Etter), names_to = "steg", values_to = "dekning") |>
  mutate(mod = factor(MNAVN[.model], rev(MNAVN[vis])),
         steg = factor(steg, c("For","Etter"))) |>
  ggplot(aes(dekning, mod)) +
  geom_vline(xintercept = 80, linetype = "22", colour = "grey55", linewidth = .4) +
  geom_line(aes(group = mod), colour = "grey75", linewidth = .9) +
  geom_point(aes(fill = steg), shape = 21, size = 2.4, colour = "#00505c", stroke = .8) +
  geom_text(aes(label = nb(dekning, 0)), vjust = -1.2, size = 2.3, colour = "grey30") +
  scale_fill_manual(values = c(For = "white", Etter = "#00505c")) +
  scale_x_continuous(labels = \(x) paste0(nb(x), " %"), limits = c(35, 100)) +
  labs(x = "Empirisk dekning (stiplet linje: nominelt 80 %)", y = NULL) +
  theme_minimal(base_size = 9) +
  theme(legend.position = "none", panel.grid.major.y = element_blank(),
        panel.grid.minor = element_blank(),
        panel.grid.major.x = element_line(linewidth = .25, colour = "grey88"),
        axis.title = element_text(size = 8, colour = "grey35"))
```

@fig-res-dekning viser dekningen før og etter kalibrering. **T4 falt, og motsatt vei av det som var forventet.** Implikasjonen sa at nominell dekning *krever* at brudd og kalender modelleres. Det stemmer ikke. M3 uten regressorer dekker `r pst(100*mean(K$dekk80[K$.model=="M3"]), 0)`, mens M6 dekker `r pst(100*mean(K$dekk80[K$.model=="M6"]), 0)`. Å modellere bruddene halverer restvariansen, intervallene krymper tilsvarende — og treffsikkerheten i sentrum følger ikke med.

@tbl-res-skjevhet viser hvorfor, og svaret er ikke det nærliggende.

```{r}
#| label: tbl-res-skjevhet
#| tbl-cap: "Dekomponering av dekningssvikten. «Skjevhet» er gjennomsnittlig logfeil — positiv betyr at modellen underpredikerer. «Forhold» er modellens eget standardavvik delt på det empiriske. Er forholdet nær 1, er intervallet riktig dimensjonert, og dekningssvikten skyldes at det står feil sted."

R |> filter(.model %in% c("M3","M5","M6")) |>
  summarise(n = n(), skjev = mean(le), t = mean(le)/(sd(le)/sqrt(n())),
            forhold = mean(sd)/sd(le), d80 = 100*mean(dekk80), .by = c(pakke_ute, .model)) |>
  arrange(pakke_ute, .model) |>
  transmute(`Pakken` = if_else(pakke_ute, "ikke estimerbar", "estimerbar"),
            Modell = MNAVN[.model], n,
            Skjevhet = pst(100*(exp(skjev)-1)), `t` = nb(t, 1),
            Forhold = nb(forhold, 2), `Dekning 80` = pst(d80, 0)) |>
  kab(align = "llrrrrr")
```

Der høstpakken ikke er estimerbar, underpredikerer M6 systematisk med `r pst(100*(exp(mean(R$le[R$.model=="M6" & R$pakke_ute]))-1))`. Full-utvalgskoeffisienten for pakken er `r pst(100*(exp(LAMBDA_ORAKEL)-1))`. Skjevheten er altså praktisk talt den utelatte koeffisienten — et lærebokeksempel på utelatt-variabel-skjevhet, her kvantifisert. Intervallene er ikke først og fremst for smale; de står på feil sted.

Det har en konsekvens for hvordan kalibreringen skal forstås. Konformal etterkalibrering løfter dekningen til `r pst(100*mean(K$kdekk80[K$.model=="M6"]), 0)`, og intervallskåren faller. Men den gjør det ved å *utvide intervallet til å dekke en systematisk feil*, ikke ved å fjerne feilen. Som korreksjon av dekning er det legitimt og virksomt; som svar på hvorfor prognosen bommer, er det symptombehandling. Den egentlige kuren er å skaffe effektstørrelsen utenfra — som er nettopp det M7 gjør.

## Verdien av et anslag, og hvor mye det er verdt {#sec-res-prior}

```{r}
#| label: tbl-res-m7
#| tbl-cap: "M7 mot M6 på de prognosepunktene der høstpakken var aktiv i målmåneden, men ikke estimerbar ved opprinnelsen. Det pålagte bidraget er her full-utvalgskoeffisienten — altså perfekt informasjon, ikke et publisert forhåndsanslag."

p7 <- R |> filter(.model %in% c("M6","M7"), pakke_ute, dato >= as.Date("2024-07-01")) |>
  select(.model, opprinnelse, h, mase, dekk80, ws80) |>
  pivot_wider(names_from = .model, values_from = c(mase, dekk80, ws80))
tibble(`Mål` = c("MASE", "Dekning ved 80 %", "Intervallskår"),
       `M6` = c(nb(mean(p7$mase_M6), 3), pst(100*mean(p7$dekk80_M6), 0), nb(mean(p7$ws80_M6))),
       `M7` = c(nb(mean(p7$mase_M7), 3), pst(100*mean(p7$dekk80_M7), 0), nb(mean(p7$ws80_M7))),
       Endring = c(pst(100*(mean(p7$mase_M7)/mean(p7$mase_M6)-1)),
                   sprintf("%+.0f p.p.", 100*(mean(p7$dekk80_M7)-mean(p7$dekk80_M6))),
                   pst(100*(mean(p7$ws80_M7)/mean(p7$ws80_M6)-1)))) |>
  kab(align = "lrrr")
```

@tbl-res-m7 gir tallene. På de `r nb(nrow(p7))` rammede punktene faller MASE med `r pst(abs(100*(mean(p7$mase_M7)/mean(p7$mase_M6)-1)))`. Forbeholdet er avgjørende og må stå først: koeffisienten som pålegges er estimert på hele utvalget, altså på data som inkluderer utfallet. Tallet måler derfor **verdien av perfekt informasjon** — en øvre grense — og ikke verdien av et faktisk forhåndsanslag. Det er ikke et resultat om konsekvensutredningers treffsikkerhet, men om hvor mye som står på spill dersom de treffer.

Det er likevel den mest direkte kvantifiseringen studien kan gi av underspørsmål **UB**: et brukbart, datert anslag for en vedtatt men ikke ikrafttrådt regelendring er verdt opptil en tredjedel av prognosefeilen i det vinduet der endringen ellers er usynlig for modellen. For høstpakken 2024 finnes ikke et slikt anslag publisert (@sec-met-prior), og M7 er derfor per i dag en demonstrasjon av mekanismen, ikke et anvendbart verktøy for den hendelsen.

## Estimerbar er ikke det samme som pålitelig {#sec-res-overgang}

Ett funn kom ut av residualanalysen og var ikke forutsett. Inngangsregelen i @sec-met-identifikasjon slipper en regressor inn så snart den er teknisk estimerbar. Men i månedene rett etter er estimatet svært upresist, som @tbl-res-overgang viser.

```{r}
#| label: tbl-res-overgang
#| tbl-cap: "Estimert koeffisient for høstpakken ved hver opprinnelse etter at inngangsregelen slipper den inn, mot antall observasjoner regressoren da har vært aktiv. Full-utvalgsestimatet er referansen nederst."

ov <- map_dfr(opprinnelser[opprinnelser >= as.Date("2024-12-01")], \(t0) {
  tr <- analyse |> mutate(mnd = yearmonth(dato)) |> filter(dato <= t0) |> as_tsibble(index = mnd)
  co <- tr |> model(m = ARIMA(log_M ~ win_covid + win_strom + pakke_2024h2 + k_pre)) |>
    tidy() |> filter(term == "pakke_2024h2")
  tibble(Opprinnelse = mnd(t0), `Aktive obs.` = sum(tr$pakke_2024h2 != 0),
         Koeffisient = nb(co$estimate, 4), `Std.feil` = nb(co$std.error, 4),
         `Nivåeffekt` = pst(100*(exp(co$estimate)-1)))
})
bind_rows(ov, tibble(Opprinnelse = "fullt utvalg", `Aktive obs.` = sum(analyse$pakke_2024h2 != 0),
                     Koeffisient = nb(LAMBDA_ORAKEL, 4), `Std.feil` = "—",
                     `Nivåeffekt` = pst(100*(exp(LAMBDA_ORAKEL)-1)))) |>
  kab(align = "lrrrr")
```

Koeffisienten svinger mellom om lag `r pst(100*(exp(min(as.numeric(str_replace(ov$Koeffisient, ",", "."))))-1))` og `r pst(100*(exp(max(as.numeric(str_replace(ov$Koeffisient, ",", "."))))-1))` i overgangsvinduet, mot `r pst(100*(exp(LAMBDA_ORAKEL)-1))` på fullt utvalg. Det forklarer den motsatte skjevheten i @tbl-res-skjevhet: der pakken *er* estimerbar, overpredikerer modellen, fordi koeffisienten i en periode er nesten dobbelt så stor som den skal være.

Konsekvensen er en presisering av protokollen som bør inn i en revidert versjon: det finnes tre regimer, ikke to. En regressor kan være *uidentifiserbar*, *identifisert men upålitelig*, eller *identifisert og stabil*. Inngangsregelen skiller bare det første fra de to siste. Det naturlige svaret er ikke en høyere terskel, men å la M7 gjøre mer arbeid: krympe mot det pålagte anslaget så lenge dataene er tynne, og slippe estimatet til etter hvert. Det er en bayesiansk formulering, og den hører hjemme i BSTS-klassen som @sec-teori-modeller allerede peker på.

## Hva som ikke holdt {#sec-res-oppgjor}

| | Implikasjon | Utfall |
|:----|:-------------------------|:-------------------------------|
| **T1** | Struktur slår naive referanser | **Holder delvis.** Slår naiv og sesongnaiv klart; ETS slår M6 samlet og signifikant på tolv måneder |
| **T2** | Eksplisitte intervensjoner hjelper | **Holder i retning.** MASE ned; Clark–West når ikke kritisk verdi på noen horisont |
| **T3** | Kalendereffekt stor før 2025, eksakt null etter | **Holder i første ledd.** Ekvivalens ikke etablert ved den forhåndssatte grensen; full styrke utelukket, halv styrke ikke |
| **T4** | Nominell dekning krever brudd og kalender | **Faller.** Modellering forverrer dekningen; konformal kalibrering reparerer den |
| **T5** | Daterte anslag bærer informasjon | **Ikke testbar som formulert.** Ingen publiserte husstandstall for hendelsen det gjaldt; demonstrert med perfekt informasjon som øvre grense |

: Utfallet holdt opp mot «faller dersom»-kolonnen i @tbl-tester, som ble skrevet før estimeringen. {#tbl-res-oppgjor}

@tbl-res-oppgjor stiller utfallet opp mot kontrakten. To av implikasjonene overlevde ikke møtet med dataene, og begge feilene er informative. T4 falt fordi den bygde på en feil intuisjon: at bedre modellering av kjente brudd også gir bedre kalibrert usikkerhet. Det motsatte skjer, fordi en modell som forklarer mer av fortiden blir mer selvsikker om framtiden uten å bli mer treffsikker der informasjonen mangler. T5 lot seg ikke teste fordi kildegrunnlaget sviktet — og at det sviktet, er i seg selv et funn om kunnskapsgrunnlaget på feltet.

Det som står igjen som studiens sterkeste enkeltresultat, er ikke et gjennomsnitt: det er at en modell med regelverkskalenderen traff et strukturelt fall på en fjerdedel innenfor tre prosent, ni måneder i forveien (@sec-res-april). Gjennomsnitt over rolige måneder skjuler den evnen. Det er nettopp derfor evalueringen er stratifisert, og nettopp derfor et samlet MASE-tall er utilstrekkelig som beslutningsgrunnlag.

# Referanser {.unnumbered}

::: {#refs}
:::
